---
phase: 10-models-settings-library-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BeatStep/Models/RunZone.swift
  - BeatStep/Models/BPMTolerance.swift
  - BeatStep/Views/Settings/SettingsView.swift
  - BeatStep/Views/Settings/ZoneSettingsRow.swift
  - BeatStep/Views/Run/TolerancePicker.swift
  - BeatStepTests/RunZoneTests.swift
  - BeatStepTests/BPMToleranceTests.swift
autonomous: true
requirements:
  - RUN-03
  - RUN-04

must_haves:
  truths:
    - "User sees BPM tolerance segments as +-3 BPM, +-7 BPM, +-12 BPM (no named labels)"
    - "User can open Settings and see Running Zones section with Z1-Z5 rows showing zone name and BPM"
    - "User can tap a zone row to reveal a Stepper and adjust the BPM value"
    - "User can tap Reset to Defaults to restore zone BPM values to compiled-in defaults"
    - "Zone BPM values persist across app restarts"
  artifacts:
    - path: "BeatStep/Models/RunZone.swift"
      provides: "Zone model with defaults, UserDefaults persistence, reset"
      contains: "struct RunZone"
    - path: "BeatStep/Models/BPMTolerance.swift"
      provides: "Updated displayName returning +-N BPM format"
      contains: "\\u{00B1}"
    - path: "BeatStep/Views/Settings/SettingsView.swift"
      provides: "Running Zones section between Account and Disconnect"
      contains: "Running Zones"
    - path: "BeatStep/Views/Settings/ZoneSettingsRow.swift"
      provides: "Zone row with tap-to-expand Stepper"
      contains: "Stepper"
    - path: "BeatStep/Views/Run/TolerancePicker.swift"
      provides: "Simplified picker with BPM Tolerance caption"
      contains: "BPM Tolerance"
  key_links:
    - from: "BeatStep/Views/Settings/SettingsView.swift"
      to: "BeatStep/Models/RunZone.swift"
      via: "@State zones array bound to RunZone.saved"
      pattern: "RunZone\\.saved"
    - from: "BeatStep/Views/Settings/ZoneSettingsRow.swift"
      to: "BeatStep/Models/RunZone.swift"
      via: "Stepper onChange triggers RunZone.saveAll"
      pattern: "RunZone\\.saveAll"
    - from: "BeatStep/Views/Run/TolerancePicker.swift"
      to: "BeatStep/Models/BPMTolerance.swift"
      via: "Picker uses displayName for segment labels"
      pattern: "displayName"
---

<objective>
Create the RunZone model with UserDefaults persistence and zone settings UI in Settings, and update tolerance picker labels to show +-N BPM format.

Purpose: Establishes the zone data model that Phase 11 will use for zone picker, and makes the tolerance picker more concrete by showing actual BPM deltas instead of named labels.
Output: RunZone.swift model, ZoneSettingsRow.swift view, updated SettingsView, updated BPMTolerance displayName, updated TolerancePicker.
</objective>

<execution_context>
@/Users/stijngragt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stijngragt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-models-settings-library-ux/10-CONTEXT.md
@.planning/phases/10-models-settings-library-ux/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From BeatStep/Models/BPMTolerance.swift:
```swift
enum BPMTolerance: String, CaseIterable {
    case tight = "tight"
    case normal = "normal"
    case loose = "loose"

    var range: Int { ... }       // 3, 7, 12
    var displayName: String { ... }  // Currently "Tight"/"Normal"/"Loose" -- CHANGE to "+-N BPM"
    var description: String { ... }  // Already "\u{00B1}N BPM"

    static var saved: BPMTolerance { ... }
    func save() { ... }
}
```

From BeatStep/Views/Run/TolerancePicker.swift:
```swift
struct TolerancePicker: View {
    @Binding var tolerance: BPMTolerance
    // Currently shows: "Tight (+-3 BPM)" -- CHANGE to just "+-3 BPM" with caption above
}
```

From BeatStep/Views/Settings/SettingsView.swift:
```swift
struct SettingsView: View {
    // Two sections: Account and Disconnect
    // Add "Running Zones" section BETWEEN them
}
```

From BeatStep/Models/PacePreset.swift (reference for zone naming pattern):
```swift
enum PacePreset: String, CaseIterable, Identifiable {
    case easyJog, steady, tempo, fast, sprint, custom
    var bpm: Int? { ... }  // 150, 160, 170, 180, 190, nil
}
```

Design tokens available:
- Color.accent (#FF4545), Color.textPrimary, Color.textSecondary
- Font.bodyText, Font.captionText, Font.subheading
- Spacing.xxs, Spacing.xs, Spacing.sm, Spacing.md
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RunZone model and BPMTolerance label update</name>
  <files>BeatStep/Models/RunZone.swift, BeatStep/Models/BPMTolerance.swift, BeatStepTests/RunZoneTests.swift, BeatStepTests/BPMToleranceTests.swift</files>
  <behavior>
    - RunZone.defaults returns 5 zones: Z1=155 Recovery, Z2=165 Endurance, Z3=174 Tempo, Z4=178 Threshold, Z5=185 Max
    - RunZone.saved returns defaults when no UserDefaults data exists
    - RunZone.saveAll persists BPM values; RunZone.saved then returns persisted values
    - RunZone.resetToDefaults clears UserDefaults; RunZone.saved returns defaults again
    - BPM values clamped to 100-220 range (stepper won't allow outside this)
    - BPMTolerance.tight.displayName == "+-3 BPM"
    - BPMTolerance.normal.displayName == "+-7 BPM"
    - BPMTolerance.loose.displayName == "+-12 BPM"
  </behavior>
  <action>
    1. Create BeatStep/Models/RunZone.swift as a struct with Identifiable, Equatable conformance:
       - Properties: `let id: Int` (1-5), `let name: String`, `var bpm: Int`
       - `static let defaults: [RunZone]` with locked values: Z1=155 Recovery, Z2=165 Endurance, Z3=174 Tempo, Z4=178 Threshold, Z5=185 Max
       - UserDefaults persistence key "runZoneBPMs" storing [String: Int] dictionary (only BPM values, not names)
       - `static var saved: [RunZone]` reads from UserDefaults, falls back to defaults
       - `static func saveAll(_ zones: [RunZone])` persists BPM dict
       - `static func resetToDefaults()` removes UserDefaults key
       - `var displayLabel: String` returning "Z1 Recovery" format

    2. Update BPMTolerance.displayName to return "\u{00B1}\(range) BPM" (same as current description property). The description property can remain as-is or be removed if unused elsewhere.

    3. Create BeatStepTests/RunZoneTests.swift testing: defaults count and values, save/load round-trip, resetToDefaults behavior.

    4. Update BeatStepTests/BPMToleranceTests.swift: add tests asserting displayName returns "+-3 BPM", "+-7 BPM", "+-12 BPM". Remove any assertions on old "Tight"/"Normal"/"Loose" display names if present.
  </action>
  <verify>
    <automated>xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:BeatStepTests/RunZoneTests -only-testing:BeatStepTests/BPMToleranceTests 2>&1 | tail -20</automated>
  </verify>
  <done>RunZone model persists and restores zone BPM values via UserDefaults. BPMTolerance.displayName returns "+-N BPM" format. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Zone settings UI and tolerance picker update</name>
  <files>BeatStep/Views/Settings/SettingsView.swift, BeatStep/Views/Settings/ZoneSettingsRow.swift, BeatStep/Views/Run/TolerancePicker.swift</files>
  <action>
    1. Create BeatStep/Views/Settings/ZoneSettingsRow.swift:
       - Accepts `@Binding var zone: RunZone`
       - Tap toggles `@State private var isExpanded = false` with `withAnimation(.easeInOut(duration: 0.2))`
       - Collapsed: HStack with "Z1 Recovery" (bodyText, textPrimary) left, "155 BPM" (bodyText, textSecondary) right
       - Expanded: adds Stepper(value: $zone.bpm, in: 100...220) with monospacedDigit label showing "\(zone.bpm) BPM" (subheading), padding top Spacing.sm

    2. Update SettingsView.swift:
       - Add `@State private var zones: [RunZone] = RunZone.saved`
       - Add "Running Zones" Section between Account and Disconnect:
         - ForEach($zones) with ZoneSettingsRow
         - "Reset to Defaults" Button (Color.accent) that sets zones = RunZone.defaults and calls RunZone.resetToDefaults()
       - Add .onChange(of: zones) that calls RunZone.saveAll(zones) to persist on each Stepper change

    3. Update TolerancePicker.swift:
       - Wrap existing Picker in VStack(spacing: Spacing.xxs)
       - Add "BPM Tolerance" caption above: Text("BPM Tolerance").font(.captionText).foregroundStyle(Color.textSecondary)
       - Simplify Picker text to just `Text(level.displayName).tag(level)` (displayName now returns "+-N BPM", drop the old description in parens)
  </action>
  <verify>
    <automated>xcodebuild build -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5</automated>
  </verify>
  <done>Settings shows Running Zones section with 5 zone rows, tap expands Stepper, Reset to Defaults works. TolerancePicker shows "BPM Tolerance" caption with "+-3 BPM"/"+-7 BPM"/"+-12 BPM" segments.</done>
</task>

</tasks>

<verification>
- `xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:BeatStepTests` passes
- Build succeeds with no warnings in modified files
- RunZone defaults match locked values: Z1=155, Z2=165, Z3=174, Z4=178, Z5=185
- BPMTolerance.displayName returns "+-N BPM" format, not named labels
</verification>

<success_criteria>
- RunZone model exists with correct defaults and UserDefaults persistence
- SettingsView has Running Zones section with Stepper-based zone editing
- TolerancePicker shows delta labels with caption
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-models-settings-library-ux/10-01-SUMMARY.md`
</output>
