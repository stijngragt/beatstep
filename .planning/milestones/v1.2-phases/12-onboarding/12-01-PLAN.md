---
phase: 12-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - project.yml
  - BeatStep/Resources/Info.plist
  - BeatStep/App/ContentView.swift
  - BeatStep/App/AppState.swift
  - BeatStep/Views/Onboarding/OnboardingFlow.swift
  - BeatStep/Views/Onboarding/OnboardingSpotifyView.swift
  - BeatStep/Views/Onboarding/OnboardingHealthView.swift
  - BeatStep/Views/Onboarding/OnboardingZonesView.swift
  - BeatStepTests/OnboardingTests.swift
autonomous: true
requirements: [ONBD-01, ONBD-02, ONBD-03]

must_haves:
  truths:
    - "First-launch user sees value-framed Spotify screen before system OAuth dialog"
    - "First-launch user sees value-framed Health/Motion screen before system permission dialogs"
    - "First-launch user sees a skippable zones explainer screen"
    - "LibraryScanService does not fire until onboarding completes"
    - "After onboarding completes, app transitions to login or authenticated state"
  artifacts:
    - path: "BeatStep/App/AppState.swift"
      provides: "AppState enum (onboarding, login, authenticated)"
      exports: ["AppState"]
    - path: "BeatStep/App/ContentView.swift"
      provides: "Root gate using AppState enum before auth check"
      contains: "switch appState"
    - path: "BeatStep/Views/Onboarding/OnboardingFlow.swift"
      provides: "Horizontal ScrollView container with forward-only navigation"
      contains: "scrollDisabled(true)"
    - path: "BeatStep/Views/Onboarding/OnboardingSpotifyView.swift"
      provides: "Value-framed Spotify connect screen"
      contains: "authService.initiateAuth()"
    - path: "BeatStep/Views/Onboarding/OnboardingHealthView.swift"
      provides: "Value-framed Health/Motion permission screen"
      contains: "requestAuthorization"
    - path: "BeatStep/Views/Onboarding/OnboardingZonesView.swift"
      provides: "Skippable zones explainer"
      contains: "onComplete"
    - path: "project.yml"
      provides: "HealthKit.framework optional link + NSHealthShareUsageDescription"
      contains: "HealthKit.framework"
  key_links:
    - from: "BeatStep/App/ContentView.swift"
      to: "BeatStep/Views/Onboarding/OnboardingFlow.swift"
      via: "AppState.onboarding case renders OnboardingFlow"
      pattern: "case .onboarding.*OnboardingFlow"
    - from: "BeatStep/Views/Onboarding/OnboardingSpotifyView.swift"
      to: "SpotifyAuthService"
      via: "initiateAuth() call on button tap"
      pattern: "authService\\.initiateAuth"
    - from: "BeatStep/Views/Onboarding/OnboardingHealthView.swift"
      to: "HealthKit"
      via: "HKHealthStore requestAuthorization"
      pattern: "requestAuthorization"
    - from: "BeatStep/App/ContentView.swift"
      to: "LibraryScanService"
      via: ".task on authenticatedView only fires after onboarding"
      pattern: "hasCompletedOnboarding.*authenticatedView"
---

<objective>
Build the 3-screen onboarding flow gated at the ContentView root level.

Purpose: First-launch users understand why BeatStep needs Spotify and Health/Motion permissions before system dialogs appear. The onboarding gate prevents LibraryScanService from firing prematurely.
Output: AppState enum, OnboardingFlow container, 3 onboarding screens, HealthKit framework integration, updated ContentView gate.
</objective>

<execution_context>
@/Users/stijngragt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stijngragt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-onboarding/12-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From BeatStep/App/ContentView.swift (CURRENT — will be modified):
```swift
struct ContentView: View {
    @Environment(SpotifyAuthService.self) private var authService
    // Currently gates only on authService.isAuthenticated
    // Must add @AppStorage("hasCompletedOnboarding") and AppState enum routing
}
```

From BeatStep/Views/Onboarding/LoginView.swift (reference for button/error patterns):
```swift
// Connect button pattern: HStack with icon + text, spotifyBrand background, pill shape
// Error view pattern: VStack with message + contextual action buttons
// Auth observation: authService.isCheckingAuth, authService.authError, authService.isAuthenticated
```

From BeatStep/Services/CadenceService.swift (motion permission pattern):
```swift
func requestPermissionAndStart() {
    let status = CMPedometer.authorizationStatus()
    // .authorized, .restricted, .denied, .notDetermined
}
```

From project.yml (current dependencies — HealthKit NOT yet present):
```yaml
dependencies:
  - package: SpotifyiOS
  - package: KeychainAccess
  - sdk: AVFoundation.framework
  - sdk: MediaPlayer.framework
  - sdk: CoreMotion.framework
  # Must add: HealthKit.framework (optional link)
```

Design tokens available (from DesignTokens.swift):
- Colors: Color.accent, .textPrimary, .textSecondary, .textTertiary, .spotifyBrand, .textOnAccent, .stateError, .stateSuccess, .stateWarning, .surfacePrimary, .surfaceSecondary
- Fonts: .heading, .subheading, .bodyText, .captionText
- Spacing: Spacing.xs, .sm, .md, .lg, .xl
- Radius: Radius.sm, .md, .lg, .pill
- Sizes: ComponentSize.iconLarge, .buttonHeight
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AppState enum, ContentView gate, and project.yml HealthKit setup</name>
  <files>BeatStep/App/AppState.swift, BeatStep/App/ContentView.swift, project.yml, BeatStepTests/OnboardingTests.swift</files>
  <behavior>
    - Test: When hasCompletedOnboarding is false, appState returns .onboarding
    - Test: When hasCompletedOnboarding is true and isAuthenticated is false, appState returns .login
    - Test: When hasCompletedOnboarding is true and isAuthenticated is true, appState returns .authenticated
    - Test: Onboarding state takes precedence over authentication state
  </behavior>
  <action>
    1. Create `BeatStep/App/AppState.swift` with the AppState enum:
       ```swift
       enum AppState {
           case onboarding
           case login
           case authenticated
       }
       ```

    2. Update `project.yml`:
       - Add under BeatStep target dependencies: `- sdk: HealthKit.framework` with `embed: false` and `link: optional`
       - Add under info.properties: `NSHealthShareUsageDescription: "BeatStep can sync with Apple Health to improve your running experience."`

    3. Update `BeatStep/App/ContentView.swift`:
       - Add `@AppStorage("hasCompletedOnboarding") private var hasCompletedOnboarding = false`
       - Add computed `appState: AppState` property that checks `hasCompletedOnboarding` FIRST, then `authService.isAuthenticated`
       - Replace the existing `if authService.isAuthenticated` branch with `switch appState` routing to OnboardingFlow / LoginView / authenticatedView
       - Gate `checkExistingAuth()` behind `hasCompletedOnboarding` in `.onAppear`
       - Keep all existing authenticatedView code unchanged (TabView, MiniPlayer, LibraryScanService task)

    4. Create `BeatStepTests/OnboardingTests.swift` with unit tests for AppState logic. Test the enum routing precedence: onboarding check before auth check. Since ContentView uses @AppStorage, test the AppState computation logic directly by creating a testable helper or testing the enum cases.

    5. Run `xcodegen generate` to regenerate the Xcode project with HealthKit dependency.
  </action>
  <verify>
    <automated>cd /Users/stijngragt/Projects/beatstep && xcodegen generate 2>&1 | tail -5 && xcodebuild build -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20</automated>
  </verify>
  <done>AppState enum exists with 3 cases. ContentView gates on hasCompletedOnboarding before isAuthenticated. project.yml includes HealthKit optional link and NSHealthShareUsageDescription. OnboardingTests exist and pass. Project builds successfully.</done>
</task>

<task type="auto">
  <name>Task 2: OnboardingFlow container and 3 onboarding screens</name>
  <files>BeatStep/Views/Onboarding/OnboardingFlow.swift, BeatStep/Views/Onboarding/OnboardingSpotifyView.swift, BeatStep/Views/Onboarding/OnboardingHealthView.swift, BeatStep/Views/Onboarding/OnboardingZonesView.swift</files>
  <action>
    1. Create `OnboardingFlow.swift`:
       - Horizontal ScrollView with `scrollDisabled(true)` to prevent free swiping
       - `@State private var scrollPosition = ScrollPosition()`
       - LazyHStack with 3 pages using `.containerRelativeFrame([.horizontal])` and `.scrollTargetLayout()`
       - `.scrollTargetBehavior(.viewAligned)` for page-snap
       - `@AppStorage("hasCompletedOnboarding") private var hasCompletedOnboarding = false`
       - Private `advanceTo(_ page: Int)` method using `withAnimation { scrollPosition.scrollTo(id: page) }`
       - Private `complete()` method that sets `hasCompletedOnboarding = true`
       - Page order: OnboardingSpotifyView (id: 0), OnboardingHealthView (id: 1), OnboardingZonesView (id: 2)
       - Dark background using `.background(Color.surfacePrimary)`

    2. Create `OnboardingSpotifyView.swift`:
       - `@Environment(SpotifyAuthService.self) private var authService`
       - `let onContinue: () -> Void`
       - Value-framing layout: SF Symbol `music.note.list` icon (size: ComponentSize.iconLarge, color: spotifyBrand), heading "Your Music Library", body text "BeatStep picks songs that match your running cadence from your Spotify playlists."
       - Connect button reusing LoginView pattern: HStack with music.note icon + "Connect with Spotify" text, spotifyBrand background, pill shape, full width
       - Error display: show `authService.authError` inline with stateError color. Include "Try Different Account" button when error contains "Premium" (same pattern as LoginView)
       - Show ProgressView when `authService.isCheckingAuth`
       - `.onChange(of: authService.isAuthenticated)` — when true, call `onContinue()`
       - Use design tokens exclusively (Spacing, Color, Font, Radius)

    3. Create `OnboardingHealthView.swift`:
       - `import HealthKit` and `import CoreMotion`
       - `let onContinue: () -> Void`
       - `@AppStorage("hasRequestedHealth") private var hasRequestedHealth = false`
       - `@AppStorage("hasRequestedMotion") private var hasRequestedMotion = false`
       - `@State private var permissionsRequested = false`
       - Value-framing layout: SF Symbol `figure.run` icon (size: ComponentSize.iconLarge, color: accent), heading "Motion & Health", body text "BeatStep uses your phone's motion sensor to detect your running cadence and match music to your pace."
       - "Allow Permissions" button (accent background, pill shape, full width) that:
         a. Calls `CadenceService.shared.requestPermissionAndStart()` for motion permission
         b. If `HKHealthStore.isHealthDataAvailable()`, calls `HKHealthStore().requestAuthorization(toShare: [], read: [HKQuantityType(.stepCount)])` with completion setting `hasRequestedHealth = true`
         c. If HealthKit not available, just sets `hasRequestedHealth = true`
         d. Sets `hasRequestedMotion = true` and `permissionsRequested = true`
       - "Skip" button below (text-only, textSecondary color) that calls `onContinue()` directly
       - When `permissionsRequested` becomes true, show "Continue" button replacing "Allow Permissions"
       - Continue button calls `onContinue()`

    4. Create `OnboardingZonesView.swift`:
       - `let onComplete: () -> Void`
       - Value-framing layout: SF Symbol `speedometer` icon (size: ComponentSize.iconLarge, color: accent), heading "Running Zones", body text "BeatStep has 5 pace zones from easy jog to fast sprint. Pick a zone and your music matches the pace."
       - Brief visual showing zone names: Z1 Recovery through Z5 Sprint with their default BPM values
       - "Get Started" button (accent background, pill shape, full width) that calls `onComplete()`
       - "Skip" text button below in textSecondary that also calls `onComplete()`
       - Use design tokens exclusively

    5. All views: dark background (`Color.surfacePrimary`), centered content with Spacers top/bottom, horizontal padding `Spacing.xl`.
  </action>
  <verify>
    <automated>cd /Users/stijngragt/Projects/beatstep && xcodegen generate 2>&1 | tail -3 && xcodebuild build -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20</automated>
  </verify>
  <done>OnboardingFlow renders 3 forward-only pages. Spotify screen shows value frame and connect button, advances on auth success. Health screen requests motion + HealthKit permissions with skip option. Zones screen shows zone overview with Get Started button. All screens use design tokens. Project builds and tests pass.</done>
</task>

</tasks>

<verification>
1. Build succeeds: `xcodebuild build -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16'`
2. Tests pass: `xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:BeatStepTests/OnboardingTests`
3. Full test suite: `xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16'`
</verification>

<success_criteria>
- Fresh install shows OnboardingFlow, not LoginView or TabView
- Spotify screen appears first with value framing and connect button
- Health/Motion screen appears second with permission request and skip option
- Zones explainer appears third with Get Started button
- After completing onboarding, app transitions to LoginView (if not yet authenticated) or TabView (if authenticated during onboarding Spotify step)
- LibraryScanService.scanEnabledPlaylists() does NOT fire during onboarding
- HealthKit.framework linked as optional in built product
</success_criteria>

<output>
After completion, create `.planning/phases/12-onboarding/12-01-SUMMARY.md`
</output>
