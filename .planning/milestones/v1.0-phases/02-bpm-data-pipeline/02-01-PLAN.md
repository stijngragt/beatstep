---
phase: 02-bpm-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BeatStep/Models/CachedBPM.swift
  - BeatStep/Models/ScannedPlaylist.swift
  - BeatStep/Models/GetSongBPMResponse.swift
  - BeatStep/Services/GetSongBPMService.swift
  - BeatStep/Services/BPMCacheService.swift
  - BeatStep/App/BeatStepApp.swift
  - BeatStepTests/GetSongBPMServiceTests.swift
  - BeatStepTests/BPMCacheServiceTests.swift
  - BeatStepTests/ModelDecodingTests.swift
  - BeatStepTests/Mocks/MockGetSongBPMResponses.swift
autonomous: true
requirements: [BPM-01, BPM-05]

must_haves:
  truths:
    - "GetSongBPM API responses decode correctly into Swift types"
    - "BPM data is cached in SwiftData and retrievable by Spotify track ID"
    - "Two-step BPM lookup (search + song detail) works with rate limiting"
    - "BPM data persists across app restarts without re-fetching from API"
    - "Tracks without BPM results are marked as lookupAttempted with nil bpm"
  artifacts:
    - path: "BeatStep/Models/CachedBPM.swift"
      provides: "SwiftData @Model for track-to-BPM cache"
      contains: "@Model"
    - path: "BeatStep/Models/ScannedPlaylist.swift"
      provides: "SwiftData @Model for user-selected running playlists"
      contains: "@Model"
    - path: "BeatStep/Models/GetSongBPMResponse.swift"
      provides: "Codable types for GetSongBPM API responses"
      contains: "GetSongBPMSearchResponse"
    - path: "BeatStep/Services/GetSongBPMService.swift"
      provides: "API client for getsongbpm.com with search and song lookup"
      exports: ["GetSongBPMService"]
    - path: "BeatStep/Services/BPMCacheService.swift"
      provides: "SwiftData CRUD for BPM cache"
      exports: ["BPMCacheService"]
  key_links:
    - from: "BeatStep/Services/GetSongBPMService.swift"
      to: "BeatStep/Models/GetSongBPMResponse.swift"
      via: "JSONDecoder decodes API responses"
      pattern: "JSONDecoder.*decode.*GetSongBPM"
    - from: "BeatStep/Services/BPMCacheService.swift"
      to: "BeatStep/Models/CachedBPM.swift"
      via: "SwiftData ModelContext operations"
      pattern: "context\\.(insert|fetch|delete)"
    - from: "BeatStep/App/BeatStepApp.swift"
      to: "BeatStep/Services/BPMCacheService.swift"
      via: "ModelContainer passed to cache service on init"
      pattern: "BPMCacheService.*setContainer"
---

<objective>
Build the BPM data foundation: SwiftData models for BPM caching, GetSongBPM API client with two-step lookup pattern, BPM cache service for local persistence, and SwiftData container initialization in the app entry point.

Purpose: Establishes the data layer that all BPM features depend on -- without this, no BPM data can be fetched, cached, or displayed.
Output: SwiftData models, GetSongBPM API service, BPM cache service, all with unit tests.
</objective>

<execution_context>
@/Users/stijngragt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stijngragt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bpm-data-pipeline/02-RESEARCH.md
@.planning/phases/02-bpm-data-pipeline/02-CONTEXT.md
@.planning/phases/02-bpm-data-pipeline/02-VALIDATION.md

<interfaces>
<!-- Key types and contracts from existing codebase. Use directly. -->

From BeatStep/Models/SpotifyTrack.swift:
```swift
struct SpotifyTrack: Codable, Identifiable, Hashable {
    let id: String
    let name: String
    let uri: String
    let durationMs: Int
    let artists: [Artist]
    let album: Album
    var artistName: String { artists.map(\.name).joined(separator: ", ") }
}
```

From BeatStep/Models/SpotifyPlaylist.swift:
```swift
struct SpotifyPlaylist: Codable, Identifiable, Hashable {
    let id: String
    let name: String
    let description: String?
    let images: [SpotifyImage]?
    let tracks: TracksRef
    let owner: PlaylistOwner?
}
```

From BeatStep/Services/SpotifyAPIService.swift:
```swift
final class SpotifyAPIService {
    static let shared = SpotifyAPIService()
    private let baseURL = "https://api.spotify.com/v1"
    func fetchPlaylists(offset: Int, limit: Int) async throws -> PaginatedResponse<SpotifyPlaylist>
    func fetchPlaylistTracks(playlistID: String, offset: Int, limit: Int) async throws -> PaginatedResponse<PlaylistTrackItem>
    // authenticatedRequest<T: Decodable>(url: URL) is private -- needs to be made internal for extension
}
```

From BeatStep/App/BeatStepApp.swift:
```swift
@main
struct BeatStepApp: App {
    @State private var authService = SpotifyAuthService.shared
    @Environment(\.scenePhase) var scenePhase
    // Currently no ModelContainer -- needs to be added
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SwiftData models, GetSongBPM response types, and model decoding tests</name>
  <files>BeatStep/Models/CachedBPM.swift, BeatStep/Models/ScannedPlaylist.swift, BeatStep/Models/GetSongBPMResponse.swift, BeatStep/App/BeatStepApp.swift, BeatStepTests/Mocks/MockGetSongBPMResponses.swift, BeatStepTests/ModelDecodingTests.swift</files>
  <behavior>
    - CachedBPM: @Model with unique spotifyTrackID, trackName, artistName, bpm (Int?), lookupAttempted (Bool), lastUpdated (Date)
    - ScannedPlaylist: @Model with unique spotifyPlaylistID, name, isEnabled (Bool), totalTracks (Int), tracksWithBPM (Int), lastScanned (Date?)
    - ScannedPlaylist.coverageText returns "N of M tracks have BPM"
    - GetSongBPMSearchResponse decodes search JSON with array of results (id, title, artist)
    - GetSongBPMSongResponse decodes song JSON with tempo as String
    - GetSongBPMTempoResponse decodes tempo endpoint JSON with array of songs
    - Mock JSON fixtures for search response, song response, and tempo response
    - ModelDecodingTests verify all mock JSON decodes into correct Swift types
  </behavior>
  <action>
    Create three model files and one test file:

    1. `CachedBPM.swift`: SwiftData @Model with `@Attribute(.unique) var spotifyTrackID: String`, `trackName: String`, `artistName: String`, `bpm: Int?` (nil means lookup returned no result), `lookupAttempted: Bool` (false = never tried), `lastUpdated: Date`. Init sets lastUpdated to Date().

    2. `ScannedPlaylist.swift`: SwiftData @Model with `@Attribute(.unique) var spotifyPlaylistID: String`, `name: String`, `isEnabled: Bool` (user toggle for "running playlist"), `totalTracks: Int`, `tracksWithBPM: Int`, `lastScanned: Date?`. Computed property `coverageText` returns "\(tracksWithBPM) of \(totalTracks) tracks have BPM".

    3. `GetSongBPMResponse.swift`: Codable structs for all GetSongBPM API response types per RESEARCH.md Pattern 3. Include: GetSongBPMSearchResponse, GetSongBPMSearchResult, GetSongBPMSongResponse, GetSongBPMSong, GetSongBPMArtist, GetSongBPMAlbum, GetSongBPMTempoResponse.

    4. Update `BeatStepApp.swift`: Add ModelContainer for CachedBPM and ScannedPlaylist schemas. Create container in init(), pass to BPMCacheService.shared.setContainer(), add .modelContainer(container) to WindowGroup. Follow RESEARCH.md Pattern 2.

    5. `MockGetSongBPMResponses.swift`: Static properties returning mock JSON Data for: a successful search response (2 results), a successful song response (with tempo "172"), a song response with no tempo (nil), and a tempo response (3 songs at BPM 170). Use raw JSON strings converted to Data.

    6. `ModelDecodingTests.swift`: XCTest class that validates the Codable response types work with mock fixtures:
       - `testSearchResponseDecoding`: Decode MockGetSongBPMResponses.searchSuccess, assert 2 results, verify first result has id and title.
       - `testSongResponseDecoding`: Decode MockGetSongBPMResponses.songSuccess, assert tempo == "172".
       - `testSongResponseNoTempo`: Decode MockGetSongBPMResponses.songNoTempo, assert tempo is nil or empty.
       - `testTempoResponseDecoding`: Decode MockGetSongBPMResponses.tempoSuccess, assert 3 songs returned.
       - `testCoverageText`: Create ScannedPlaylist(tracksWithBPM: 5, totalTracks: 10), assert coverageText == "5 of 10 tracks have BPM".

    **Important:** Import SwiftData in model files. The app init must use try! for ModelContainer (crash on schema error is acceptable for v1).
  </action>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:BeatStepTests/ModelDecodingTests 2>&1 | grep -E '(Test Case|Tests? (passed|failed)|error:)' | tail -20</automated>
  </verify>
  <done>All three model files compile, mock JSON decodes correctly into Swift types, ScannedPlaylist.coverageText works, BeatStepApp initializes ModelContainer with both schemas, all ModelDecodingTests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GetSongBPMService and BPMCacheService with tests</name>
  <files>BeatStep/Services/GetSongBPMService.swift, BeatStep/Services/BPMCacheService.swift, BeatStepTests/GetSongBPMServiceTests.swift, BeatStepTests/BPMCacheServiceTests.swift</files>
  <behavior>
    - GetSongBPMService.fetchBPM(title:artist:) performs two-step lookup: search then song detail
    - GetSongBPMService.fetchSongsByBPM(_:) queries /tempo/ endpoint
    - Rate limiting: 300ms delay between API calls
    - Title sanitization: strips " - Remastered", " - Live", " (feat. ...)" etc before searching
    - BPMCacheService.cache(trackID:name:artist:bpm:) inserts or updates CachedBPM in SwiftData
    - BPMCacheService.getBPM(forTrackID:) returns Int? from cache
    - BPMCacheService.hasLookup(forTrackID:) returns true if CachedBPM exists for that ID
    - BPMCacheService.coverageStats(forPlaylistTracks:) returns (withBPM: Int, total: Int)
    - Tests use mock JSON (not live API) and in-memory SwiftData container
  </behavior>
  <action>
    1. `GetSongBPMService.swift`: Singleton service following established codebase pattern. Store API key in a static constant (can be moved to config later). Implement:
       - `fetchBPM(title: String, artist: String) async throws -> Int?`: Sanitize title (strip suffixes like " - Remastered", " - Live Version", " (feat. ...)", " [Deluxe]" using regex). URL-encode query. GET `/search/?api_key=KEY&type=song&lookup=QUERY`. Decode GetSongBPMSearchResponse. If no results, return nil. Wait 300ms (Task.sleep). GET `/song/?api_key=KEY&id=FIRST_RESULT_ID`. Decode GetSongBPMSongResponse. Parse tempo string to Int, return nil if unparseable.
       - `fetchSongsByBPM(_ bpm: Int) async throws -> [GetSongBPMSong]`: GET `/tempo/?api_key=KEY&bpm=BPM`. Decode GetSongBPMTempoResponse. Return tempo array.
       - Make the URLSession injectable for testing (init parameter with default .shared).

    2. `BPMCacheService.swift`: `@MainActor` singleton. Private `container: ModelContainer?`, public `setContainer(_:)`. Computed `context` returns `container!.mainContext`. Implement:
       - `cache(trackID: String, name: String, artist: String, bpm: Int?)`: Fetch existing CachedBPM by trackID. If exists, update bpm and lookupAttempted. If not, insert new CachedBPM with lookupAttempted=true. Save context.
       - `getBPM(forTrackID: String) -> Int?`: Fetch CachedBPM by trackID, return bpm.
       - `hasLookup(forTrackID: String) -> Bool`: FetchDescriptor with predicate on spotifyTrackID, return count > 0.
       - `coverageStats(forTrackIDs: [String]) -> (withBPM: Int, total: Int)`: Fetch all CachedBPMs matching IDs, count those with non-nil bpm.

    3. `GetSongBPMServiceTests.swift`: Test search response decoding from mock JSON. Test song response decoding (with tempo, without tempo). Test tempo response decoding. Test title sanitization (strip " - Remastered 2024", " (feat. Artist)", " - Live"). Use MockGetSongBPMResponses fixtures.

    4. `BPMCacheServiceTests.swift`: Create in-memory ModelContainer in setUp. Test cache insert and retrieve. Test cache update (same trackID, new bpm). Test hasLookup returns false for unknown ID, true for cached. Test coverageStats counts correctly. Test that caching with nil bpm still sets lookupAttempted to true.

    **API key:** Use a placeholder constant `private static let apiKey = "GETSONGBPM_API_KEY"` with a comment to replace before testing with live API. Store in GetSongBPMService for now.

    **Note on tests:** Use `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer` prefix for all xcodebuild commands. Use `iPhone 17 Pro` simulator (not iPhone 16 -- see Phase 1 deviation).
  </action>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:BeatStepTests/GetSongBPMServiceTests -only-testing:BeatStepTests/BPMCacheServiceTests 2>&1 | grep -E '(Test Case|Tests? (passed|failed)|error:)' | tail -20</automated>
  </verify>
  <done>GetSongBPMService decodes all response types, handles title sanitization, rate-limits calls. BPMCacheService performs CRUD on SwiftData with correct lookupAttempted tracking. All unit tests pass.</done>
</task>

</tasks>

<verification>
1. Project builds without errors: `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild build -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -quiet`
2. All new tests pass: `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:BeatStepTests/GetSongBPMServiceTests -only-testing:BeatStepTests/BPMCacheServiceTests -only-testing:BeatStepTests/ModelDecodingTests`
3. All existing tests still pass: `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 17 Pro'`
</verification>

<success_criteria>
- SwiftData models CachedBPM and ScannedPlaylist exist and compile
- GetSongBPM API response types decode correctly from mock JSON
- GetSongBPMService performs two-step BPM lookup with rate limiting
- BPMCacheService persists and retrieves BPM data via SwiftData
- BeatStepApp initializes ModelContainer for both models
- All unit tests green (new + existing)
</success_criteria>

<output>
After completion, create `.planning/phases/02-bpm-data-pipeline/02-01-SUMMARY.md`
</output>
