---
phase: 05-guided-run-polish
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - BeatStep/Models/RunMode.swift
  - BeatStep/Models/RampPhase.swift
  - BeatStep/Models/PacePreset.swift
  - BeatStep/Models/GetSongBPMResponse.swift
  - BeatStep/Models/CachedBPM.swift
  - BeatStep/Services/GetSongBPMService.swift
  - BeatStep/Services/BPMCacheService.swift
  - BeatStep/Services/RunEngineService.swift
  - BeatStep/Services/BPMDiscoveryService.swift
  - BeatStepTests/RunEngineServiceTests.swift
  - BeatStepTests/PacePresetTests.swift
autonomous: true
requirements: [RUN-02, RUN-03, BPM-06]

must_haves:
  truths:
    - "Guided mode selects songs at fixed target BPM, not runner's cadence"
    - "Warm-up ramp steps BPM from 140 toward target, one step per song"
    - "Cool-down ramp steps BPM from target back toward 140"
    - "Ramp never overshoots target BPM (clamped)"
    - "Smart selection ranks matches by danceability (high for active, low for ramp)"
    - "When pool has fewer than 3 matches, discovery fires asynchronously"
    - "Named pace presets map to correct BPM values"
    - "RunMode and target BPM persist to UserDefaults"
  artifacts:
    - path: "BeatStep/Models/RunMode.swift"
      provides: "RunMode enum (free/guided) with UserDefaults persistence"
      contains: "enum RunMode"
    - path: "BeatStep/Models/RampPhase.swift"
      provides: "RampPhase enum (warmUp/atPace/coolDown) with display labels"
      contains: "enum RampPhase"
    - path: "BeatStep/Models/PacePreset.swift"
      provides: "Named pace presets with BPM values"
      contains: "enum PacePreset"
    - path: "BeatStep/Services/RunEngineService.swift"
      provides: "Guided mode orchestration, ramp state machine, smart selection, discovery integration"
      contains: "effectiveBPM"
    - path: "BeatStepTests/RunEngineServiceTests.swift"
      provides: "Tests for guided mode, ramp, smart selection"
      min_lines: 50
    - path: "BeatStepTests/PacePresetTests.swift"
      provides: "Tests for pace preset BPM mapping"
      min_lines: 10
  key_links:
    - from: "BeatStep/Services/RunEngineService.swift"
      to: "BeatStep/Models/RunMode.swift"
      via: "runMode property drives effectiveBPM"
      pattern: "runMode"
    - from: "BeatStep/Services/RunEngineService.swift"
      to: "BeatStep/Services/BPMDiscoveryService.swift"
      via: "on-demand discovery when pool < 3"
      pattern: "discoverTracks"
    - from: "BeatStep/Services/RunEngineService.swift"
      to: "danceabilityMap"
      via: "smart selection ranking"
      pattern: "danceabilityMap"
---

<objective>
Build the guided run engine: RunMode/RampPhase/PacePreset models, extend RunEngineService with guided mode (fixed target BPM), warm-up/cool-down ramp state machine, danceability-based smart selection, and on-demand catalog discovery. Add danceability field to GetSongBPM response parsing and BPM cache.

Purpose: This is the core brain of Phase 5. All three requirements (RUN-02, RUN-03, BPM-06) are implemented here as testable logic before UI wiring in Plan 02.
Output: All models, extended engine, extended cache, passing tests.
</objective>

<execution_context>
@/Users/stijngragt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stijngragt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-guided-run-polish/05-CONTEXT.md
@.planning/phases/05-guided-run-polish/05-RESEARCH.md

@BeatStep/Services/RunEngineService.swift
@BeatStep/Models/BPMTolerance.swift
@BeatStep/Models/CachedBPM.swift
@BeatStep/Models/GetSongBPMResponse.swift
@BeatStep/Services/GetSongBPMService.swift
@BeatStep/Services/BPMCacheService.swift
@BeatStep/Services/BPMDiscoveryService.swift
@BeatStepTests/RunEngineServiceTests.swift

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From BeatStep/Models/BPMTolerance.swift:
```swift
enum BPMTolerance: String, CaseIterable {
    case tight, normal, loose
    var range: Int  // tight=3, normal=7, loose=12
    var displayName: String
    static var saved: BPMTolerance
    func save()
}
```

From BeatStep/Services/RunEngineService.swift:
```swift
@Observable final class RunEngineService {
    static let shared = RunEngineService()
    var isRunActive: Bool
    var tolerance: BPMTolerance
    var currentMatchedTrack: SpotifyTrack?
    func startRun(playlist: SpotifyPlaylist, tracks: [SpotifyTrack]) async
    func stopRun()
    func skipToNextMatch() async
    func findMatchingTracks(forSPM spm: Int) -> [SpotifyTrack]
    func selectNextMatch(forSPM spm: Int) -> SpotifyTrack?
    func evaluateCadenceChange(newSPM: Int) -> Bool
    func loadForTesting(tracks: [SpotifyTrack], bpmMap: [String: Int])
    func setSustainedSPMForTesting(_ spm: Int)
}
```

From BeatStep/Services/BPMCacheService.swift:
```swift
@MainActor final class BPMCacheService {
    static let shared: BPMCacheService
    func cache(trackID: String, name: String, artist: String, bpm: Int?)
    func getBPM(forTrackID trackID: String) -> Int?
}
```

From BeatStep/Services/BPMDiscoveryService.swift:
```swift
@MainActor final class BPMDiscoveryService {
    static let shared: BPMDiscoveryService
    func discoverTracks(atBPM bpm: Int) async throws -> [SpotifyTrack]
    func saveToDiscoveryPlaylist(tracks: [SpotifyTrack]) async throws
}
```

From BeatStep/Models/GetSongBPMResponse.swift:
```swift
struct GetSongBPMSong: Codable {
    let id: String
    let title: String?
    let tempo: String?
    let artist: GetSongBPMArtist?
    let album: GetSongBPMAlbum?
    // NOTE: danceability field needs to be added
}
```

From BeatStep/Models/CachedBPM.swift:
```swift
@Model final class CachedBPM {
    @Attribute(.unique) var spotifyTrackID: String
    var trackName: String
    var artistName: String
    var bpm: Int?
    var lookupAttempted: Bool
    var lastUpdated: Date
    // NOTE: danceability field needs to be added
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - Models + failing tests for guided mode, ramp, smart selection</name>
  <files>
    BeatStep/Models/RunMode.swift,
    BeatStep/Models/RampPhase.swift,
    BeatStep/Models/PacePreset.swift,
    BeatStep/Models/GetSongBPMResponse.swift,
    BeatStep/Models/CachedBPM.swift,
    BeatStepTests/RunEngineServiceTests.swift,
    BeatStepTests/PacePresetTests.swift
  </files>
  <behavior>
    - PacePreset.easyJog.bpm == 150, .steady.bpm == 160, .tempo.bpm == 170, .fast.bpm == 180, .sprint.bpm == 190
    - RunMode.saved persists to UserDefaults (same pattern as BPMTolerance.saved)
    - RunMode.savedTargetBPM persists target BPM int to UserDefaults
    - RampPhase.warmUp.displayLabel == "Warming up", .atPace.displayLabel == "At pace", .coolDown.displayLabel == "Cooling down"
    - Guided mode: engine.selectNextMatch uses targetBPM, not sustainedSPM
    - Warm-up ramp: effectiveTargetBPM starts at 140, steps +8 per song, clamps at target
    - Cool-down ramp: effectiveTargetBPM starts at target, steps -8 per song, clamps at 140
    - Smart selection: among matches, higher danceability preferred when preferHighEnergy=true, lower when false
    - Missing danceability defaults to 50 (neutral ranking)
    - When matches.count < 3 and not already discovering, discovery flag is set
  </behavior>
  <action>
    1. Create RunMode.swift:
       - `enum RunMode: String, CaseIterable { case free, guided }` with UserDefaults persistence following BPMTolerance.saved/.save() pattern
       - Add `static var savedTargetBPM: Int` (get/set, default 160) with separate UserDefaults key "selectedTargetBPM"

    2. Create RampPhase.swift:
       - `enum RampPhase: String { case warmUp, atPace, coolDown }`
       - `var displayLabel: String` returning "Warming up" / "At pace" / "Cooling down" per user decision

    3. Create PacePreset.swift:
       - `enum PacePreset: String, CaseIterable, Identifiable { case easyJog, steady, tempo, fast, sprint, custom }`
       - `var bpm: Int?` (nil for custom)
       - `var displayName: String` ("Easy Jog", "Steady", "Tempo", "Fast", "Sprint", "Custom")
       - Values: easyJog=150, steady=160, tempo=170, fast=180, sprint=190

    4. Add `let danceability: Int?` to GetSongBPMSong in GetSongBPMResponse.swift (Codable handles nil automatically)

    5. Add `var danceability: Int? = nil` to CachedBPM.swift (SwiftData lightweight migration handles optional with nil default)

    6. Create PacePresetTests.swift with tests for all preset BPM values and display names

    7. Extend RunEngineServiceTests.swift with FAILING tests:
       - testGuidedModeUsesTargetBPM: set runMode=.guided(targetBPM:170), setSustainedSPMForTesting(120), selectNextMatch should match at 170 not 120
       - testWarmUpRampProgression: effectiveTargetBPM starts at 140, after 1 song = 148, after 2 = 156, etc.
       - testCoolDownRampProgression: effectiveTargetBPM from target steps down by 8 per song
       - testRampClampsToTarget: warm-up from 140 toward 175, should clamp at 175 not overshoot to 180
       - testSmartSelectionRanksByDanceability: with danceabilityMap set, preferHighEnergy=true selects highest danceability first
       - testSmartSelectionLowDanceabilityForRamp: preferHighEnergy=false selects lowest danceability
       - testMissingDanceabilityFallback: track without danceability in map treated as 50

    All tests MUST FAIL (RED phase). Commit: `test(05-01): add failing tests for guided mode, ramp, and smart selection`
  </action>
  <verify>
    <automated>xcodebuild test -project BeatStep.xcodeproj -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:BeatStepTests/PacePresetTests 2>&1 | tail -20</automated>
    PacePresetTests should PASS (pure model tests). New RunEngineServiceTests should FAIL (engine not yet extended).
  </verify>
  <done>All 3 model files created with correct enums. GetSongBPMSong has danceability field. CachedBPM has danceability field. PacePresetTests pass. 7+ new RunEngineServiceTests exist and fail. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - Implement guided mode, ramp, smart selection, discovery in RunEngineService</name>
  <files>
    BeatStep/Services/RunEngineService.swift,
    BeatStep/Services/GetSongBPMService.swift,
    BeatStep/Services/BPMCacheService.swift
  </files>
  <action>
    Extend RunEngineService to make all RED tests pass:

    1. Add observable properties:
       - `var runMode: RunMode = .saved` (observable, UI reads this)
       - `var rampPhase: RampPhase? = nil` (nil in free mode, observable for UI phase label)
       - Add @ObservationIgnored private properties:
         - `private var targetBPM: Int = 160`
         - `private var rampSongsPlayed: Int = 0`
         - `private var danceabilityMap: [String: Int] = [:]`
         - `private var isDiscovering: Bool = false`

    2. Add `effectiveBPM` computed property:
       - Free mode: returns sustainedSPM (existing behavior)
       - Guided mode: returns ramp-calculated BPM based on rampPhase and rampSongsPlayed
       - Warm-up: `min(140 + rampSongsPlayed * 8, targetBPM)` -- clamp at target
       - At pace: targetBPM
       - Cool-down: `max(targetBPM - rampSongsPlayed * 8, 140)` -- clamp at 140

    3. Modify `startRun` to accept optional `mode: RunMode = .free`:
       - If guided: set targetBPM from mode, rampPhase = .warmUp, rampSongsPlayed = 0
       - Load danceability data into danceabilityMap alongside bpmMap (from BPMCacheService)
       - Use effectiveBPM for first song selection instead of sustainedSPM

    4. Replace `matches.randomElement()!` in selectNextMatch with smart selection:
       - Determine `preferHighEnergy`: true during free run active and guided atPace, false during warmUp/coolDown
       - Sort matches by danceability (from danceabilityMap, fallback 50 for missing)
       - If preferHighEnergy: descending sort, pick from top 3 randomly (variety)
       - If not preferHighEnergy: ascending sort, pick from top 3 randomly
       - This applies to BOTH free run and guided run per user decision

    5. After song selection in queueNextMatch, handle ramp transitions:
       - Increment rampSongsPlayed after each song in guided mode
       - If warmUp and effectiveBPM >= targetBPM: transition to .atPace, reset rampSongsPlayed
       - If coolDown and effectiveBPM <= 140: call stopRun() (auto-stop per research recommendation)

    6. Add `startCoolDown()` method:
       - Sets rampPhase = .coolDown, rampSongsPlayed = 0
       - Next song selection will use decreasing BPM

    7. In queueNextMatch, use effectiveBPM instead of sustainedSPM for guided mode
       - Free mode cadence monitor stays the same (sustainedSPM drives matching)
       - Guided mode ignores cadence for song selection (uses ramp target)

    8. Add on-demand discovery integration:
       - After filtering matches in selectNextMatch, if matches.count < 3 and !isDiscovering:
         - Set isDiscovering = true
         - Fire background Task calling BPMDiscoveryService.shared.discoverTracks(atBPM: effectiveBPM)
         - On success: add discovered tracks to playlistTracks + bpmMap, save to discovery playlist
         - Set isDiscovering = false when done
       - Don't await discovery in the selection path (non-blocking)

    9. Extend BPMCacheService:
       - Add `func getDanceability(forTrackID:) -> Int?` that reads CachedBPM.danceability
       - Add `func cacheDanceability(trackID:danceability:)` for updating existing cache entries

    10. Extend GetSongBPMService.fetchBPM to also return danceability:
        - Change return type to `(bpm: Int?, danceability: Int?)` or add a separate method
        - Simpler: add `func fetchBPMAndDanceability(title:artist:) async throws -> (bpm: Int?, danceability: Int?)` that parses both from the song response
        - Keep existing fetchBPM for backward compatibility

    11. Update stopRun() to reset all new state (runMode back to .saved is NOT needed -- persist across runs)

    12. Add testing helpers:
        - `func setDanceabilityMapForTesting(_ map: [String: Int])` to inject danceability data
        - `func setRunModeForTesting(_ mode: RunMode, targetBPM: Int)` to set guided mode in tests
        - `func setRampPhaseForTesting(_ phase: RampPhase, songsPlayed: Int)` for ramp testing

    Commit: `feat(05-01): implement guided mode, ramp, smart selection, and discovery`
  </action>
  <verify>
    <automated>xcodebuild test -project BeatStep.xcodeproj -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:BeatStepTests/RunEngineServiceTests 2>&1 | tail -30</automated>
    All RunEngineServiceTests pass (existing 11 + new 7+). No regressions.
  </verify>
  <done>All RED tests now pass. Free run behavior unchanged (no regressions). Guided mode selects at target BPM. Ramp steps BPM correctly with clamping. Smart selection ranks by danceability. Discovery fires when pool < 3. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR - Full test suite verification and cleanup</name>
  <files>
    BeatStep/Services/RunEngineService.swift,
    BeatStepTests/RunEngineServiceTests.swift
  </files>
  <action>
    Run full test suite to verify no regressions across all test files. If any cleanup needed (extract methods, improve naming, remove duplication), do it. Verify that:
    - All existing BPMToleranceTests still pass
    - All existing RunEngineServiceTests still pass
    - New PacePresetTests pass
    - New guided/ramp/smart-selection tests pass
    - No compilation warnings in modified files

    If code is clean, no changes needed -- just verify and document.

    Commit (only if changes made): `refactor(05-01): clean up guided mode implementation`
  </action>
  <verify>
    <automated>xcodebuild test -project BeatStep.xcodeproj -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:BeatStepTests 2>&1 | tail -40</automated>
    Full test suite passes with 0 failures.
  </verify>
  <done>Full test suite green. No regressions. All Phase 5 engine logic tested and working.</done>
</task>

</tasks>

<verification>
- All existing Phase 4 tests pass (no regressions in free run behavior)
- New guided mode tests pass (selectNextMatch uses targetBPM in guided mode)
- Ramp progression tests pass (warm-up steps up, cool-down steps down, both clamp correctly)
- Smart selection tests pass (danceability ranking with fallback to 50)
- PacePreset tests pass (all presets map to correct BPM values)
- `xcodebuild test -project BeatStep.xcodeproj -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:BeatStepTests 2>&1 | tail -40` -- all tests pass
</verification>

<success_criteria>
- RunMode, RampPhase, PacePreset models exist with correct values and persistence
- GetSongBPMSong parses danceability field from API response
- CachedBPM stores danceability (lightweight migration, optional with nil default)
- RunEngineService supports guided mode with effectiveBPM computed property
- Ramp state machine transitions correctly (warmUp -> atPace, coolDown -> auto-stop)
- Smart selection replaces randomElement() in both free and guided modes
- On-demand discovery fires asynchronously when pool < 3 matches
- 25+ tests pass with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/05-guided-run-polish/05-01-SUMMARY.md`
</output>
