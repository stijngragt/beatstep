---
phase: 06-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: []
autonomous: false
requirements: [DS-05]

must_haves:
  truths:
    - "User has reviewed the complete token palette (colors, fonts, spacing) and approved it"
    - "Phase 8 view migration is gated on this approval"
  artifacts: []
  key_links: []
---

<objective>
Present the design system tokens for user review and approval before any view migration begins.

Purpose: DS-05 requires explicit user approval of token definitions. This gate ensures the visual foundation is correct before Phase 8 applies tokens to all views.
Output: User approval recorded. Phase 6 complete.
</objective>

<execution_context>
@/Users/stijngragt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stijngragt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-design-system-foundation/06-01-SUMMARY.md
@BeatStep/DesignSystem/DesignTokens.swift
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Design token approval gate (DS-05)</name>
  <files>BeatStep/DesignSystem/DesignTokens.swift</files>
  <action>
    Read DesignTokens.swift and present a formatted token review document to the user showing:
    1. Color palette with hex values and semantic names (accent, backgrounds, text, state colors)
    2. Typography scale with sizes, weights, and font designs
    3. Spacing and radius values
    4. Component sizing constants

    Also build and run the app in the simulator so the user can verify dark mode enforcement visually.
    Launch with: `xcodebuild build -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16'` then open simulator.

    Present the what-built summary:
    - Color palette: accent (#FF4545 heartbeat red), 3 near-black background levels, 3 text opacity levels, state colors, SpotifyBrand green
    - Typography scale: SF Pro for text (heading 22pt bold, body 16pt, caption 13pt), SF Pro Rounded for numeric displays (hero 52pt, secondary 18pt)
    - Spacing: 2/4/8/16/24/32/48pt scale
    - Radii: 6/12/20/28pt
    - Component sizing: mini-player 64pt, button 52pt, icons 24/44/60pt

    Ask user to verify:
    1. Review the token summary -- are colors, fonts, and spacing values correct for BeatStep's identity?
    2. Check the running app in the simulator -- is the entire UI dark? No white flashes? Do system alerts appear dark?
    3. Confirm they are happy with these token definitions as the foundation for all future view work.

    Resume signal: Type "approved" to approve tokens and complete Phase 6, or describe any changes needed.
  </action>
  <verify>
    <automated>echo "DS-05: Checkpoint -- requires human approval"</automated>
  </verify>
  <done>User has explicitly approved the design token definitions. Phase 6 complete, unblocking Phase 7 and Phase 8 view migration.</done>
</task>

</tasks>

<verification>
User explicitly approves the token definitions. DS-05 requirement satisfied.
</verification>

<success_criteria>
- User has seen the complete token palette and explicitly approved it
- No changes requested (or requested changes have been applied and re-approved)
- Phase 6 is marked complete, unblocking Phase 7 and Phase 8
</success_criteria>

<output>
After completion, create `.planning/phases/06-design-system-foundation/06-02-SUMMARY.md`
</output>
