---
phase: 31-settings-skeleton-states
plan: 03
type: execute
wave: 1
depends_on: ["31-02"]
files_modified:
  - BeatStep/Views/Library/PlaylistListView.swift
  - BeatStep/Views/Library/PlaylistDetailView.swift
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [POL-03, POL-04]

must_haves:
  truths:
    - "Skeleton-to-content transition uses explicit .transition(.opacity) for guaranteed opacity crossfade"
    - "POL-03 and POL-04 are defined in REQUIREMENTS.md with traceability rows"
  artifacts:
    - path: "BeatStep/Views/Library/PlaylistListView.swift"
      provides: "Explicit opacity transition on skeleton and content branches"
      contains: ".transition(.opacity)"
    - path: "BeatStep/Views/Library/PlaylistDetailView.swift"
      provides: "Explicit opacity transition on skeleton and content branches"
      contains: ".transition(.opacity)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "POL-03 and POL-04 requirement definitions and traceability"
      contains: "POL-03"
  key_links:
    - from: "PlaylistListView.swift"
      to: "BSAnimation.smooth"
      via: ".animation modifier + .transition(.opacity)"
      pattern: "\\.transition\\(\\.opacity\\)"
    - from: "PlaylistDetailView.swift"
      to: "BSAnimation.smooth"
      via: ".animation modifier + .transition(.opacity)"
      pattern: "\\.transition\\(\\.opacity\\)"
---

<objective>
Close two verification gaps from phase 31: (1) add explicit `.transition(.opacity)` to skeleton/content branches in both library views for guaranteed opacity crossfade, and (2) add POL-03 and POL-04 definitions to REQUIREMENTS.md with traceability rows.

Purpose: Ensure skeleton-to-content transitions are true opacity crossfades (not layout spring animations), and complete the requirements traceability chain.
Output: Two updated Swift files with `.transition(.opacity)`, one updated REQUIREMENTS.md with Polish section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-settings-skeleton-states/31-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .transition(.opacity) to skeleton and content branches</name>
  <files>BeatStep/Views/Library/PlaylistListView.swift, BeatStep/Views/Library/PlaylistDetailView.swift</files>
  <read_first>BeatStep/Views/Library/PlaylistListView.swift, BeatStep/Views/Library/PlaylistDetailView.swift</read_first>
  <action>
In PlaylistListView.swift, inside the `body` computed property, find the Group containing the if/else branches:

```swift
Group {
    if isLoading && playlists.isEmpty {
        PlaylistListSkeleton()
    } else if let error, playlists.isEmpty {
        errorView(message: error)
    } else {
        playlistList
    }
}
.animation(BSAnimation.smooth, value: isLoading)
```

Add `.transition(.opacity)` to the skeleton branch and the content branches:

```swift
Group {
    if isLoading && playlists.isEmpty {
        PlaylistListSkeleton()
            .transition(.opacity)
    } else if let error, playlists.isEmpty {
        errorView(message: error)
            .transition(.opacity)
    } else {
        playlistList
            .transition(.opacity)
    }
}
.animation(BSAnimation.smooth, value: isLoading)
```

Apply the identical pattern in PlaylistDetailView.swift. Find the Group:

```swift
Group {
    if isLoading && tracks.isEmpty {
        PlaylistDetailSkeleton()
    } else if let error, tracks.isEmpty {
        errorView(message: error)
    } else {
        trackList
    }
}
.animation(BSAnimation.smooth, value: isLoading)
```

Add `.transition(.opacity)` to each branch:

```swift
Group {
    if isLoading && tracks.isEmpty {
        PlaylistDetailSkeleton()
            .transition(.opacity)
    } else if let error, tracks.isEmpty {
        errorView(message: error)
            .transition(.opacity)
    } else {
        trackList
            .transition(.opacity)
    }
}
.animation(BSAnimation.smooth, value: isLoading)
```

Do NOT change anything else in either file. The `.animation(BSAnimation.smooth, value: isLoading)` on the Group stays as-is.
  </action>
  <verify>
    <automated>grep -n "\.transition(\.opacity)" BeatStep/Views/Library/PlaylistListView.swift BeatStep/Views/Library/PlaylistDetailView.swift | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep ".transition(.opacity)" PlaylistListView.swift returns at least 3 matches (skeleton, error, content branches)
    - grep ".transition(.opacity)" PlaylistDetailView.swift returns at least 3 matches (skeleton, error, content branches)
    - grep ".animation(BSAnimation.smooth" both files still returns matches (not removed)
    - No other lines changed in either file
  </acceptance_criteria>
  <done>Both PlaylistListView.swift and PlaylistDetailView.swift have explicit .transition(.opacity) on all branches inside the loading Group, ensuring guaranteed opacity crossfade driven by BSAnimation.smooth</done>
</task>

<task type="auto">
  <name>Task 2: Add POL-03 and POL-04 to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md</read_first>
  <action>
In .planning/REQUIREMENTS.md, add a new "### Polish" section after the "### Run Modes" section (before "### Skip Queue"). Add these two requirement definitions:

```markdown
### Polish

- [x] **POL-03**: Loading states use shimmer skeleton placeholders instead of ProgressView spinners for library views
- [x] **POL-04**: Settings screen organized into grouped sections (Account, Run Defaults, Permissions, Debug, About) with SF Symbol icons and discoverable structure
```

Mark both as [x] since they are already implemented and verified.

Then in the Traceability table at the bottom of the file, add two rows before the existing `SKIP-01` row:

```markdown
| POL-03 | Phase 31 | Complete |
| POL-04 | Phase 31 | Complete |
```

Update the Coverage section to reflect the new count:
- Change `v1 requirements: 18 total` to `v1 requirements: 20 total`
- Change `Mapped to phases: 18` to `Mapped to phases: 20`

Update `Last updated` at the bottom to `2026-03-26 after phase 31 gap closure`.
  </action>
  <verify>
    <automated>grep -c "POL-0[34]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "POL-03" REQUIREMENTS.md returns matches in both the definition section and traceability table (at least 2 matches)
    - grep "POL-04" REQUIREMENTS.md returns matches in both the definition section and traceability table (at least 2 matches)
    - grep "v1 requirements: 20 total" REQUIREMENTS.md returns 1 match
    - grep "Mapped to phases: 20" REQUIREMENTS.md returns 1 match
    - The "### Polish" section exists between "### Run Modes" and "### Skip Queue"
  </acceptance_criteria>
  <done>REQUIREMENTS.md contains formal definitions for POL-03 and POL-04, traceability rows mapping both to Phase 31 with Complete status, and updated coverage counts</done>
</task>

</tasks>

<verification>
1. `grep -n ".transition(.opacity)" BeatStep/Views/Library/PlaylistListView.swift` shows 3 matches
2. `grep -n ".transition(.opacity)" BeatStep/Views/Library/PlaylistDetailView.swift` shows 3 matches
3. `grep -n "POL-03\|POL-04" .planning/REQUIREMENTS.md` shows at least 4 matches (2 definitions + 2 traceability rows)
4. `grep ".animation(BSAnimation.smooth" BeatStep/Views/Library/PlaylistListView.swift BeatStep/Views/Library/PlaylistDetailView.swift` still returns matches (not removed)
</verification>

<success_criteria>
- Both library views use explicit .transition(.opacity) on all loading state branches
- POL-03 and POL-04 are formally defined in REQUIREMENTS.md with traceability
- No orphaned requirement IDs remain
- Existing .animation(BSAnimation.smooth) modifier preserved on both Group wrappers
</success_criteria>

<output>
After completion, create `.planning/phases/31-settings-skeleton-states/31-03-SUMMARY.md`
</output>
