---
phase: 22-sensor-lab
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - BeatStep/Views/Settings/SensorLabView.swift
  - BeatStep/Views/Settings/SettingsView.swift
autonomous: false
requirements:
  - SLAB-01
  - SLAB-02
  - SLAB-03
  - SLAB-04
must_haves:
  truths:
    - "Tapping app version text 5 times toggles Sensor Lab visibility in Settings"
    - "Sensor Lab shows live accelerometer X/Y/Z, cadence SPM, step count, and algorithm state"
    - "Slider adjusts detection interval from 0.5s to 5.0s with visible effect"
    - "Real-time waveform chart displays accelerometer magnitude over time"
    - "Navigating away from Sensor Lab stops the accelerometer"
  artifacts:
    - path: "BeatStep/Views/Settings/SensorLabView.swift"
      provides: "Debug screen with accelerometer data, cadence readout, waveform chart, interval slider"
      contains: "struct SensorLabView"
      min_lines: 60
    - path: "BeatStep/Views/Settings/SettingsView.swift"
      provides: "Hidden toggle via 5-tap gesture on version text, conditional NavigationLink to SensorLabView"
      contains: "sensorLabEnabled"
  key_links:
    - from: "BeatStep/Views/Settings/SensorLabView.swift"
      to: "BeatStep/Services/SensorLabService.swift"
      via: "SensorLabService.shared property access"
      pattern: "SensorLabService\\.shared"
    - from: "BeatStep/Views/Settings/SensorLabView.swift"
      to: "BeatStep/Services/CadenceService.swift"
      via: "CadenceService.shared for cadence/state readout"
      pattern: "CadenceService\\.shared"
    - from: "BeatStep/Views/Settings/SettingsView.swift"
      to: "BeatStep/Views/Settings/SensorLabView.swift"
      via: "NavigationLink conditionally shown when sensorLabEnabled"
      pattern: "NavigationLink.*SensorLabView"
    - from: "BeatStep/Views/Settings/SensorLabView.swift"
      to: "SensorLabService.stopAccelerometer"
      via: ".onDisappear modifier"
      pattern: "\\.onDisappear"
---

<objective>
Build the SensorLabView debug screen and wire the hidden toggle into SettingsView.

Purpose: Give developers and power users a live window into the cadence detection algorithm -- accelerometer data, cadence state, and a waveform chart -- accessible via a hidden Settings toggle.
Output: Complete Sensor Lab UI with real-time data display, waveform chart, interval slider, and hidden activation gesture.
</objective>

<execution_context>
@/Users/stijngragt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stijngragt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-sensor-lab/22-RESEARCH.md
@.planning/phases/22-sensor-lab/22-01-SUMMARY.md

@BeatStep/Views/Settings/SettingsView.swift
@BeatStep/DesignSystem/DesignTokens.swift
</context>

<interfaces>
<!-- From Plan 01 (SensorLabService created there) -->

From BeatStep/Models/AccelerometerSample.swift:
```swift
struct AccelerometerSample: Identifiable {
    let id: UUID
    let timestamp: TimeInterval
    let x: Double
    let y: Double
    let z: Double
    var magnitude: Double { sqrt(x*x + y*y + z*z) }
}
```

From BeatStep/Services/SensorLabService.swift:
```swift
@Observable
final class SensorLabService {
    static let shared = SensorLabService()
    var accelerationX: Double
    var accelerationY: Double
    var accelerationZ: Double
    var stepCount: Int
    var isRunning: Bool
    var detectionInterval: TimeInterval  // 0.5 to 5.0
    var samples: [AccelerometerSample]
    func startAccelerometer()
    func stopAccelerometer()
    func updateInterval(_ newInterval: TimeInterval)
}
```

From BeatStep/Services/CadenceService.swift:
```swift
@Observable
final class CadenceService {
    static let shared = CadenceService()
    var currentSPM: Int
    var state: CadenceState  // .idle, .detecting, .active, .paused
}
```

From BeatStep/Views/Settings/SettingsView.swift (current structure):
- List-based layout with Section groups: Account, Running Zones, Playback, Permissions, Disconnect
- Uses @AppStorage for persisted toggles
- NavigationLink pattern for sub-screens
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SensorLabView + AccelerometerChartView + hidden Settings toggle</name>
  <files>BeatStep/Views/Settings/SensorLabView.swift, BeatStep/Views/Settings/SettingsView.swift</files>
  <action>
    Create SensorLabView.swift in BeatStep/Views/Settings/ with:

    1. **Accelerometer section**: LabeledContent rows showing X, Y, Z formatted to 4 decimal places with " g" suffix
    2. **Cadence section**: LabeledContent rows for SPM (from CadenceService.shared.currentSPM), State (from CadenceService.shared.state, use String(describing:)), Steps (from SensorLabService.shared.stepCount)
    3. **Waveform section**: Inline AccelerometerChartView (can be a private struct in same file or separate -- keep in same file for simplicity). Uses Swift Charts `import Charts`, LineMark plotting samples by timestamp (x) and magnitude (y). Apply `.chartYScale(domain: 0...3)` and `.drawingGroup()` for Metal-backed rendering. Frame height 200. Use Color.accent for line color.
    4. **Detection Interval section**: Slider bound to service.detectionInterval via Binding(get:set:) calling service.updateInterval on set. Range 0.5...5.0, step 0.5. Show current value as Text with .captionText font and .textSecondary color.
    5. **Lifecycle**: `.onAppear { SensorLabService.shared.startAccelerometer() }` and `.onDisappear { SensorLabService.shared.stopAccelerometer() }` -- critical for battery drain prevention.
    6. **Navigation**: `.navigationTitle("Sensor Lab")` and `.navigationBarTitleDisplayMode(.inline)`
    7. Use List layout with sections, matching SettingsView pattern.

    Modify SettingsView.swift:
    1. Add `@AppStorage("sensorLabEnabled") private var sensorLabEnabled = false`
    2. Add `@State private var debugTapCount = 0`
    3. After the Disconnect section, add a new Section that conditionally shows `NavigationLink("Sensor Lab") { SensorLabView() }` when sensorLabEnabled is true
    4. At the bottom of the List (after last Section), add a footer-style Text showing "BeatStep v1.4" with .captionText font and .textSecondary color, centered. Attach `.onTapGesture` that increments debugTapCount; when debugTapCount >= 5, toggle sensorLabEnabled and reset debugTapCount to 0.
    5. Use `.frame(maxWidth: .infinity)` on the version text for centering.

    Add SensorLabView.swift to the Xcode project's BeatStep target.
  </action>
  <verify>
    <automated>cd /Users/stijngragt/Projects/beatstep && xcodebuild build -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5</automated>
  </verify>
  <done>
    - SensorLabView displays accelerometer X/Y/Z, cadence SPM, state, step count, waveform chart, and interval slider
    - SettingsView has hidden 5-tap toggle on version text that reveals/hides Sensor Lab NavigationLink
    - Accelerometer starts on appear and stops on disappear
    - Project builds without errors or warnings
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Sensor Lab</name>
  <files>BeatStep/Views/Settings/SensorLabView.swift, BeatStep/Views/Settings/SettingsView.swift</files>
  <action>
    Human verifies the complete Sensor Lab debug screen works correctly in the simulator.

    What was built: Complete Sensor Lab debug screen with hidden Settings toggle, live accelerometer data, cadence readout, real-time waveform chart, and configurable detection interval.

    How to verify:
    1. Open the app in Simulator, navigate to Settings tab
    2. Scroll to bottom, tap the "BeatStep v1.4" text 5 times rapidly -- "Sensor Lab" link should appear
    3. Tap "Sensor Lab" to open the debug screen
    4. Verify sections visible: Accelerometer (X/Y/Z showing 0.0000 g in simulator), Cadence (SPM, State, Steps), Waveform (empty chart area), Detection Interval (slider)
    5. Adjust the interval slider -- value label should update
    6. Go back to Settings -- Sensor Lab link should still be visible (persisted)
    7. Tap version text 5 more times -- Sensor Lab link should disappear (toggled off)
    Note: Accelerometer data will be zeros in simulator (no hardware). Real data requires a physical device.
  </action>
  <verify>User confirms visual and functional correctness</verify>
  <done>User approves Sensor Lab UI layout, hidden toggle behavior, and interval slider interaction</done>
</task>

</tasks>

<verification>
- `xcodebuild build -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16'` succeeds
- `xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16'` full suite passes (no regressions)
- Hidden toggle persists across app launches via @AppStorage
- Sensor Lab view starts/stops accelerometer on appear/disappear
</verification>

<success_criteria>
Sensor Lab is a fully functional debug screen accessible via hidden Settings toggle, displaying live accelerometer data, cadence state, real-time waveform chart, and configurable detection interval. Closing the screen stops the accelerometer to prevent battery drain.
</success_criteria>

<output>
After completion, create `.planning/phases/22-sensor-lab/22-02-SUMMARY.md`
</output>
