---
phase: 29-run-menu-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BeatStep/Models/RunZone.swift
  - BeatStepTests/RunZoneTests.swift
  - BeatStepTests/ZoneSelectionTests.swift
autonomous: true
requirements:
  - RUN-02

must_haves:
  truths:
    - "RunZone.selectedZoneIds persists a Set<Int> to UserDefaults and round-trips correctly"
    - "mergedBPMRange(for:) returns floor...ceiling from selected zones"
    - "Empty selectedZoneIds returns nil merged range (free mode)"
    - "Migration from single selectedZoneId to selectedZoneIds works on first read"
  artifacts:
    - path: "BeatStep/Models/RunZone.swift"
      provides: "Multi-zone selection model with Set<Int> persistence and mergedBPMRange"
      contains: "selectedZoneIds"
    - path: "BeatStepTests/RunZoneTests.swift"
      provides: "Unit tests for mergedBPMRange computation"
      contains: "testMergedBPMRange"
    - path: "BeatStepTests/ZoneSelectionTests.swift"
      provides: "Unit tests for Set<Int> persistence and migration"
      contains: "selectedZoneIds"
  key_links:
    - from: "BeatStep/Models/RunZone.swift"
      to: "UserDefaults"
      via: "Array<Int> encoding for Set<Int>"
      pattern: "UserDefaults.standard.set\\(Array"
---

<objective>
Add multi-zone selection model to RunZone: `selectedZoneIds: Set<Int>` with UserDefaults persistence (encoded as sorted Array<Int>), migration from the old single `selectedZoneId: Int?`, and a `mergedBPMRange(for:)` static method that returns the floor...ceiling ClosedRange<Int> from selected zones.

Purpose: RUN-02 requires multi-zone selection. The model layer must exist and be tested before the UI can be rebuilt.
Output: Updated RunZone.swift with new API, extended test files with new test cases.
</objective>

<execution_context>
@/Users/stijngragt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stijngragt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-run-menu-rebuild/29-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From BeatStep/Models/RunZone.swift:
```swift
struct RunZone: Identifiable, Equatable {
    let id: Int
    let name: String
    var bpm: Int
    var displayLabel: String { "Z\(id) \(name)" }
    static let defaults: [RunZone]  // 5 zones: Recovery(155), Endurance(165), Tempo(174), Threshold(178), Max(185)
    static var saved: [RunZone]     // reads from UserDefaults, falls back to defaults
    static func saveAll(_ zones: [RunZone])
    static func resetToDefaults()
    // Current single-select (to be kept for backward compat, but deprecated):
    static var selectedZoneId: Int?  // UserDefaults key: "selectedRunZoneId"
}
```

From BeatStep/Models/BPMTolerance.swift:
```swift
enum BPMTolerance: String, CaseIterable {
    case tight, normal, loose
    var range: Int  // 3, 7, 12
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add multi-zone selection model and tests</name>
  <files>BeatStep/Models/RunZone.swift, BeatStepTests/RunZoneTests.swift, BeatStepTests/ZoneSelectionTests.swift</files>
  <behavior>
    - selectedZoneIds round-trip: save Set([1, 3]), read back Set([1, 3])
    - selectedZoneIds defaults to empty Set when no data stored
    - Migration: when selectedZoneIds key missing but selectedZoneId = 3, selectedZoneIds returns Set([3])
    - Migration: when both keys missing, selectedZoneIds returns empty Set
    - mergedBPMRange(for: Set([1, 3])) returns 155...174 (Recovery floor to Tempo ceiling)
    - mergedBPMRange(for: Set([2])) returns 165...165 (single zone)
    - mergedBPMRange(for: Set()) returns nil (empty = free mode)
    - mergedBPMRange(for: Set([1, 5])) returns 155...185 (full range)
  </behavior>
  <action>
Write tests FIRST in ZoneSelectionTests.swift and RunZoneTests.swift, then implement:

**ZoneSelectionTests.swift** -- Add new test cases (keep existing tests for backward compat):
- `testSelectedZoneIdsDefaultsToEmptySet` -- remove both keys, assert empty
- `testSelectedZoneIdsRoundTrip` -- save Set([1, 3]), read back, assert equal
- `testSelectedZoneIdsMigrationFromSingleId` -- set old "selectedRunZoneId" to 3, remove "selectedRunZoneIds" key, assert selectedZoneIds == Set([3])
- `testSelectedZoneIdsEmptySetPersists` -- save empty set, read back, assert empty
- Update tearDown to also remove "selectedRunZoneIds" key

**RunZoneTests.swift** -- Add new test cases:
- `testMergedBPMRangeMultiZone` -- Set([1, 3]) -> 155...174
- `testMergedBPMRangeSingleZone` -- Set([2]) -> 165...165
- `testMergedBPMRangeEmptyReturnsNil` -- Set() -> nil
- `testMergedBPMRangeFullSpread` -- Set([1, 5]) -> 155...185

**RunZone.swift** -- Add below existing selectedZoneId property:
1. `private static let selectedIdsKey = "selectedRunZoneIds"`
2. `static var selectedZoneIds: Set<Int>` computed property:
   - GET: Try `UserDefaults.standard.array(forKey: selectedIdsKey) as? [Int]`, convert to Set. If nil, check old `selectedZoneId` for migration (return `[selectedZoneId]` if non-nil, else empty Set).
   - SET: `UserDefaults.standard.set(Array(newValue).sorted(), forKey: selectedIdsKey)`
3. `static func mergedBPMRange(for zoneIds: Set<Int>) -> ClosedRange<Int>?`:
   - Filter `saved` zones by zoneIds, get min/max of bpm values
   - Return nil if empty, else `min...max`

Do NOT remove the old `selectedZoneId` property -- it stays for migration reads.
  </action>
  <verify>
    <automated>xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:BeatStepTests/ZoneSelectionTests -only-testing:BeatStepTests/RunZoneTests 2>&1 | tail -20</automated>
  </verify>
  <done>All new test cases pass. selectedZoneIds persists Set<Int> via UserDefaults. mergedBPMRange returns correct floor...ceiling range. Migration from single selectedZoneId works.</done>
</task>

</tasks>

<verification>
- `xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:BeatStepTests/ZoneSelectionTests -only-testing:BeatStepTests/RunZoneTests` -- all tests green
- RunZone.swift contains `selectedZoneIds` and `mergedBPMRange` APIs
</verification>

<success_criteria>
- Set<Int> round-trip persistence works (save/load through UserDefaults)
- Migration from old Int? selectedZoneId to Set<Int> selectedZoneIds works
- mergedBPMRange returns correct floor...ceiling for any zone combination
- Empty selection returns nil (free mode signal)
- All existing RunZone and ZoneSelection tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/29-run-menu-rebuild/29-01-SUMMARY.md`
</output>
