---
phase: 25-consolidate-run-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BeatStep/Views/Library/PlaylistDetailView.swift
  - BeatStep/Views/Run/RunView.swift
  - BeatStep.xcodeproj/project.pbxproj
autonomous: false
requirements: [FLOW-01, FLOW-03, FLOW-04]

must_haves:
  truths:
    - "Tapping 'Run with this playlist' in PlaylistDetailView switches to Run tab with that playlist pre-loaded"
    - "The old RunView.swift file no longer exists in the codebase"
    - "There is no way to start a run from any screen other than the Run tab"
    - "After navigating from Library to Run tab, user sees the selected playlist and can start immediately"
  artifacts:
    - path: "BeatStep/Views/Library/PlaylistDetailView.swift"
      provides: "CTA button and tab switching, no RunView NavigationLink"
      contains: "Run with this playlist"
    - path: "BeatStep/Views/Run/RunView.swift"
      provides: "DELETED - must not exist"
  key_links:
    - from: "BeatStep/Views/Library/PlaylistDetailView.swift"
      to: "LastRunPlaylist + selectedTab"
      via: "CTA button writes LastRunPlaylist then sets selectedTab = .run"
      pattern: "LastRunPlaylist\\.id.*=.*playlist\\.id"
    - from: "BeatStep/Views/Run/RunTabView.swift"
      to: "LastRunPlaylist"
      via: "fetchPlaylistIfNeeded on .onAppear picks up new playlist"
      pattern: "fetchPlaylistIfNeeded"
---

<objective>
Consolidate run entry to the Run tab. Add a "Run with this playlist" CTA button to PlaylistDetailView that switches to the Run tab with the playlist pre-loaded, then delete RunView.swift and all references to it.

Purpose: Eliminate duplicate run initiation paths -- the Run tab becomes the single entry point for all runs.
Output: PlaylistDetailView with CTA button, RunView.swift deleted, clean Xcode project.
</objective>

<execution_context>
@/Users/stijngragt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stijngragt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-consolidate-run-entry/25-CONTEXT.md
@.planning/phases/24-fix-run-tab-start/24-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From BeatStep/App/ContentView.swift:
```swift
enum Tab: Hashable {
    case library
    case run
    case settings
}
// ContentView passes $selectedTab to RunTabView
// selectedTab needs to be accessible from PlaylistDetailView (via Environment or binding)
```

From BeatStep/Models/LastRunPlaylist.swift:
```swift
enum LastRunPlaylist {
    static var name: String? { get set }
    static var id: String? { get set }
    static var imageURL: String? { get set }
}
```

From BeatStep/Views/Run/RunTabView.swift (line 239-253):
```swift
// Start Run button styling reference for CTA visual consistency:
private var startRunButton: some View {
    Button {
        startRun()
    } label: {
        Text("Start Run")
            .font(.title3.weight(.bold))
            .foregroundStyle(Color.textOnAccent)
            .frame(maxWidth: .infinity)
            .padding(.vertical, Spacing.md)
            .background(RoundedRectangle(cornerRadius: Radius.lg).fill(Color.accent))
    }
}
```

From BeatStep/Views/Library/PlaylistDetailView.swift:
```swift
struct PlaylistDetailView: View {
    let playlist: SpotifyPlaylist
    // Lines 32-37: NavigationLink to RunView in toolbar (TO BE REMOVED)
    // Lines 112-157: playlistHeader VStack (CTA goes below track count HStack)
}
```

Xcode project RunView references (both must be removed from project.pbxproj):
- PBXBuildFile: `96335669F0A8AD75E3EB5206 /* RunView.swift in Sources */`
- PBXFileReference: `51071A444A70CF8FE4921C0E /* RunView.swift */`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CTA button and wire Library-to-Run-tab navigation</name>
  <files>BeatStep/Views/Library/PlaylistDetailView.swift</files>
  <action>
Modify PlaylistDetailView to add a "Run with this playlist" CTA button and remove the RunView NavigationLink.

1. Add a `@Binding var selectedTab: Tab` property to PlaylistDetailView. Update the call site in PlaylistListView to pass the binding (check PlaylistListView.swift for the NavigationLink that creates PlaylistDetailView -- it will need the binding passed through). Use an EnvironmentKey or propagate the binding from ContentView through the Library NavigationStack. The cleanest approach: add an `@Environment(\.selectedTab)` custom environment key so PlaylistDetailView can read/write tab selection without deep binding chains. Define the EnvironmentKey in ContentView.swift (or a small extension file) and inject `.environment(\.selectedTab, $selectedTab)` on the Library NavigationStack in ContentView.

2. In the toolbar (lines 32-37), remove the NavigationLink to RunView entirely. Keep only the Scan BPM and Clear BPM buttons.

3. In the playlistHeader VStack (after the track count HStack around line 154), add a full-width CTA button:
   - Text: "Run with this playlist"
   - Icon: "figure.run" leading
   - Styling: matches Start Run button on RunTabView -- `.font(.title3.weight(.bold))`, `.foregroundStyle(Color.textOnAccent)`, `.frame(maxWidth: .infinity)`, `.padding(.vertical, Spacing.md)`, `.background(RoundedRectangle(cornerRadius: Radius.lg).fill(Color.accent))`, `.padding(.horizontal, Spacing.xl)`
   - On tap: write `LastRunPlaylist.name = playlist.name`, `LastRunPlaylist.id = playlist.id`, `LastRunPlaylist.imageURL = playlist.images?.first?.url`, then set `selectedTab = .run` (via environment)
   - No toast, no animation delay -- instant tab switch

4. The Library NavigationStack stays on PlaylistDetailView after the switch -- do NOT pop or reset navigation.
  </action>
  <verify>
    <automated>cd /Users/stijngragt/Projects/beatstep && DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild build -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>PlaylistDetailView has a prominent "Run with this playlist" CTA button that writes LastRunPlaylist and switches to Run tab. The old NavigationLink to RunView is gone from the toolbar. Project compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Delete RunView.swift and remove all references</name>
  <files>BeatStep/Views/Run/RunView.swift, BeatStep.xcodeproj/project.pbxproj</files>
  <action>
1. Delete the file `BeatStep/Views/Run/RunView.swift` from disk.

2. Remove RunView references from `BeatStep.xcodeproj/project.pbxproj`:
   - Remove the PBXBuildFile line containing `96335669F0A8AD75E3EB5206 /* RunView.swift in Sources */`
   - Remove the PBXFileReference line containing `51071A444A70CF8FE4921C0E /* RunView.swift */`
   - Remove the file reference `51071A444A70CF8FE4921C0E` from the PBXGroup children list where it appears
   - Remove `96335669F0A8AD75E3EB5206` from the PBXSourcesBuildPhase files list

3. Grep the entire BeatStep directory for any remaining `RunView` references (excluding `ActiveRunView`). There should be zero matches. If any remain, fix them.
  </action>
  <verify>
    <automated>cd /Users/stijngragt/Projects/beatstep && ! test -f BeatStep/Views/Run/RunView.swift && ! grep -r "RunView" BeatStep/ --include="*.swift" | grep -v "ActiveRunView" | grep -v "ActiveRunViewTests" && DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild build -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>RunView.swift is deleted, all references removed from Xcode project, no remaining RunView references in Swift files (only ActiveRunView remains), project compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify consolidated run entry flow</name>
  <files>N/A</files>
  <action>
Human verifies the full Library-to-Run-tab flow and confirms RunView is gone.
What was built: Library-to-Run-tab routing via CTA button, and RunView deletion. The Run tab is now the only way to start a run.
How to verify:
    1. Open app on simulator, go to Library tab
    2. Tap a playlist to open PlaylistDetailView
    3. Verify: No run icon in toolbar (old NavigationLink gone)
    4. Verify: "Run with this playlist" CTA button visible in playlist header
    5. Tap "Run with this playlist"
    6. Verify: App instantly switches to Run tab with that playlist shown
    7. Verify: Can tap Start Run and a run begins
    8. Stop the run, switch back to Library tab
    9. Verify: PlaylistDetailView is still showing (navigation preserved)
  </action>
  <verify>Human confirms all 9 verification steps pass</verify>
  <done>User approves the consolidated run entry flow</done>
</task>

</tasks>

<verification>
- RunView.swift does not exist on disk
- No Swift file references RunView (only ActiveRunView)
- PlaylistDetailView contains "Run with this playlist" button
- PlaylistDetailView writes LastRunPlaylist and switches tab on CTA tap
- Xcode project builds without errors
- Full flow: Library > PlaylistDetail > CTA > Run tab > Start Run works
</verification>

<success_criteria>
1. Tapping "Run with this playlist" in PlaylistDetailView navigates to Run tab with that playlist pre-loaded
2. The old RunView.swift is deleted and no references remain
3. There is no way to start a run from any screen other than the Run tab
4. After navigating from Library to Run tab, user sees the selected playlist and can start immediately
</success_criteria>

<output>
After completion, create `.planning/phases/25-consolidate-run-entry/25-01-SUMMARY.md`
</output>
