---
phase: 32-micro-interaction-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - BeatStep/Views/Settings/SettingsView.swift
  - BeatStep/Views/Settings/RunDefaultsView.swift
  - BeatStep/Views/Settings/SensorLabView.swift
  - BeatStep/Views/Run/RunTabView.swift
  - BeatStep/Views/Player/RunPlayerView.swift
  - BeatStep/Views/Player/MiniPlayerView.swift
  - BeatStep/Views/Onboarding/OnboardingSpotifyView.swift
  - BeatStep/Views/Onboarding/OnboardingHealthView.swift
  - BeatStep/Views/Onboarding/OnboardingPlaylistView.swift
  - BeatStep/Views/Onboarding/OnboardingZonesView.swift
autonomous: true
requirements:
  - POL-02
must_haves:
  truths:
    - "Every button tap provides haptic feedback"
    - "Destructive actions (Disconnect Spotify, Reset Defaults) use warning haptic"
    - "Success confirmations (Start Run, BPM save, scan complete, onboarding complete) use success haptic"
    - "Standard button taps use light haptic"
    - "Picker/toggle changes use selection haptic"
  artifacts:
    - path: "BeatStep/Views/Settings/SettingsView.swift"
      provides: "Haptics on Disconnect Spotify (warning), Open Settings (light), version tap (selection)"
      contains: "BSHaptics"
    - path: "BeatStep/Views/Run/RunTabView.swift"
      provides: "Haptics on Start Run (success), Go to Library (light), Retry (light)"
      contains: "BSHaptics.success()"
    - path: "BeatStep/Views/Player/RunPlayerView.swift"
      provides: "Haptics on play/pause (light), skip (light)"
      contains: "BSHaptics.light()"
    - path: "BeatStep/Views/Onboarding/OnboardingZonesView.swift"
      provides: "Haptics on Get Started (success), Skip (light)"
      contains: "BSHaptics.success()"
  key_links:
    - from: "BeatStep/Views/Settings/SettingsView.swift"
      to: "BSHaptics"
      via: "import and static method calls"
      pattern: "BSHaptics\\.(warning|light|selection)"
    - from: "BeatStep/Views/Run/RunTabView.swift"
      to: "BSHaptics"
      via: "import and static method calls"
      pattern: "BSHaptics\\.(success|light)"
---

<objective>
Add haptic feedback to every interactive element across Settings, Run tab, Player, and Onboarding views.

Purpose: Make every button, picker, and toggle in the app feel responsive with appropriate haptic feedback following the mapping defined in decisions D-01 through D-05.
Output: 10 view files updated with BSHaptics calls on all interactive elements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-micro-interaction-pass/32-CONTEXT.md
@.planning/phases/32-micro-interaction-pass/32-RESEARCH.md

<interfaces>
From BeatStep/DesignSystem/BSHaptics.swift:
```swift
enum BSHaptics {
    static func light()      // Standard button taps (D-01)
    static func medium()     // Medium impact
    static func heavy()      // Heavy impact
    static func selection()  // Picker/toggle changes (D-02)
    static func success()    // Success confirmations (D-04)
    static func warning()    // Destructive actions (D-03)
    static func error()      // Error notifications
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add haptics to Settings, RunDefaults, and SensorLab views</name>
  <files>
    BeatStep/Views/Settings/SettingsView.swift,
    BeatStep/Views/Settings/RunDefaultsView.swift,
    BeatStep/Views/Settings/SensorLabView.swift
  </files>
  <read_first>
    BeatStep/Views/Settings/SettingsView.swift,
    BeatStep/Views/Settings/RunDefaultsView.swift,
    BeatStep/Views/Settings/SensorLabView.swift,
    BeatStep/DesignSystem/BSHaptics.swift
  </read_first>
  <action>
    **SettingsView.swift (per D-01, D-02, D-03):**
    1. "Disconnect Spotify" button: Add `BSHaptics.warning()` as the first line of the button action (destructive action, D-03)
    2. "Open Settings" button (system settings link): Add `BSHaptics.light()` as the first line of the button action (standard tap, D-01)
    3. Version label 5-tap easter egg: Add `BSHaptics.selection()` on each tap increment (D-02, selection-style feedback for each tap)

    **RunDefaultsView.swift (per D-02, D-03):**
    1. "Reset to Defaults" button: Add `BSHaptics.warning()` as the first line of the button action (destructive action, D-03)
    2. Each Picker's `.onChange` handler: Add `BSHaptics.selection()` as the first line (picker change, D-02). Apply to any zone/tolerance/tempo mode pickers in this view.

    **SensorLabView.swift (per D-02, low priority debug view):**
    1. Any Slider `.onChange` handlers: Add `BSHaptics.selection()` (value change, D-02)
  </action>
  <verify>
    <automated>
grep -n "BSHaptics" BeatStep/Views/Settings/SettingsView.swift BeatStep/Views/Settings/RunDefaultsView.swift BeatStep/Views/Settings/SensorLabView.swift
    </automated>
  </verify>
  <acceptance_criteria>
    - SettingsView.swift contains `BSHaptics.warning()` (Disconnect Spotify button)
    - SettingsView.swift contains `BSHaptics.light()` (Open Settings button)
    - SettingsView.swift contains `BSHaptics.selection()` (version tap easter egg)
    - RunDefaultsView.swift contains `BSHaptics.warning()` (Reset to Defaults button)
    - RunDefaultsView.swift contains `BSHaptics.selection()` (picker onChange)
    - SensorLabView.swift contains `BSHaptics.selection()` (slider onChange)
  </acceptance_criteria>
  <done>All Settings, RunDefaults, and SensorLab interactive elements have appropriate haptic feedback.</done>
</task>

<task type="auto">
  <name>Task 2: Add haptics to RunTab, Player, and Onboarding views</name>
  <files>
    BeatStep/Views/Run/RunTabView.swift,
    BeatStep/Views/Player/RunPlayerView.swift,
    BeatStep/Views/Player/MiniPlayerView.swift,
    BeatStep/Views/Onboarding/OnboardingSpotifyView.swift,
    BeatStep/Views/Onboarding/OnboardingHealthView.swift,
    BeatStep/Views/Onboarding/OnboardingPlaylistView.swift,
    BeatStep/Views/Onboarding/OnboardingZonesView.swift
  </files>
  <read_first>
    BeatStep/Views/Run/RunTabView.swift,
    BeatStep/Views/Player/RunPlayerView.swift,
    BeatStep/Views/Player/MiniPlayerView.swift,
    BeatStep/Views/Onboarding/OnboardingSpotifyView.swift,
    BeatStep/Views/Onboarding/OnboardingHealthView.swift,
    BeatStep/Views/Onboarding/OnboardingPlaylistView.swift,
    BeatStep/Views/Onboarding/OnboardingZonesView.swift,
    BeatStep/DesignSystem/BSHaptics.swift
  </read_first>
  <action>
    **RunTabView.swift (per D-01, D-04):**
    1. "Start Run" button: Add `BSHaptics.success()` as the first line of the button action (run start confirmation, D-04)
    2. "Go to Library" button: Add `BSHaptics.light()` (standard tap, D-01)
    3. "Retry" button (error state): Add `BSHaptics.light()` (standard tap, D-01)

    **RunPlayerView.swift (per D-01, D-05):**
    1. Play/Pause button: Add `BSHaptics.light()` before `onPlayPause()` call (user action during run, D-01/D-05)
    2. Skip button: Add `BSHaptics.light()` before `onSkip()` call (user action during run, D-01/D-05)

    **MiniPlayerView.swift (per D-01):**
    1. Play/Pause button: Add `BSHaptics.light()` before the play/pause toggle action (standard tap, D-01)
    2. Skip button: Add `BSHaptics.light()` before the skip action (standard tap, D-01)

    **OnboardingSpotifyView.swift (per D-01):**
    1. "Connect with Spotify" button: Add `BSHaptics.light()` (standard tap, D-01)
    2. "Try Different Account" button: Add `BSHaptics.light()` (standard tap, D-01)

    **OnboardingHealthView.swift (per D-01):**
    1. "Allow Permissions" button: Add `BSHaptics.light()` (standard tap, D-01)
    2. "Continue" button: Add `BSHaptics.light()` (standard tap, D-01)
    3. "Skip" button: Add `BSHaptics.light()` (standard tap, D-01)

    **OnboardingPlaylistView.swift (per D-01, D-04):**
    1. Playlist row tap: Add `BSHaptics.light()` (standard tap, D-01)
    2. "Continue" button (analysis complete state): Add `BSHaptics.success()` (scan complete confirmation, D-04)

    **OnboardingZonesView.swift (per D-01, D-04):**
    1. "Get Started" button: Add `BSHaptics.success()` (onboarding complete, D-04)
    2. "Skip" button: Add `BSHaptics.light()` (standard tap, D-01)
  </action>
  <verify>
    <automated>
# Verify BSHaptics usage in all target files
grep -c "BSHaptics\." BeatStep/Views/Run/RunTabView.swift BeatStep/Views/Player/RunPlayerView.swift BeatStep/Views/Player/MiniPlayerView.swift BeatStep/Views/Onboarding/OnboardingSpotifyView.swift BeatStep/Views/Onboarding/OnboardingHealthView.swift BeatStep/Views/Onboarding/OnboardingPlaylistView.swift BeatStep/Views/Onboarding/OnboardingZonesView.swift

# Verify success haptic on run start
grep -n "BSHaptics.success" BeatStep/Views/Run/RunTabView.swift

# Verify light haptic on player controls
grep -n "BSHaptics.light" BeatStep/Views/Player/RunPlayerView.swift BeatStep/Views/Player/MiniPlayerView.swift
    </automated>
  </verify>
  <acceptance_criteria>
    - RunTabView.swift contains `BSHaptics.success()` (Start Run) and `BSHaptics.light()` (Go to Library, Retry)
    - RunPlayerView.swift contains `BSHaptics.light()` at least 2 times (play/pause + skip)
    - MiniPlayerView.swift contains `BSHaptics.light()` at least 2 times (play/pause + skip)
    - OnboardingSpotifyView.swift contains `BSHaptics.light()` at least 2 times
    - OnboardingHealthView.swift contains `BSHaptics.light()` at least 3 times (allow, continue, skip)
    - OnboardingPlaylistView.swift contains `BSHaptics.light()` and `BSHaptics.success()`
    - OnboardingZonesView.swift contains `BSHaptics.success()` and `BSHaptics.light()`
  </acceptance_criteria>
  <done>Every button in RunTab, Player, and Onboarding views has appropriate haptic feedback. Success haptic on run start and onboarding completion. Light haptic on all standard button taps.</done>
</task>

</tasks>

<verification>
```bash
# Count BSHaptics usage across all view files
grep -rc "BSHaptics\." BeatStep/Views/ | grep -v ":0$"

# Verify haptic mapping correctness
grep -n "BSHaptics.warning" BeatStep/Views/Settings/SettingsView.swift BeatStep/Views/Settings/RunDefaultsView.swift
grep -n "BSHaptics.success" BeatStep/Views/Run/RunTabView.swift BeatStep/Views/Onboarding/OnboardingZonesView.swift BeatStep/Views/Onboarding/OnboardingPlaylistView.swift
```
</verification>

<success_criteria>
- All 10 target files contain BSHaptics calls
- Destructive actions use BSHaptics.warning() (SettingsView disconnect, RunDefaultsView reset)
- Success confirmations use BSHaptics.success() (RunTabView start, OnboardingZonesView get started, OnboardingPlaylistView continue)
- Standard buttons use BSHaptics.light()
- Pickers/toggles use BSHaptics.selection()
</success_criteria>

<output>
After completion, create `.planning/phases/32-micro-interaction-pass/32-02-SUMMARY.md`
</output>
