---
phase: 18-bpm-confidence-model
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - BeatStepTests/BPMCacheServiceTests.swift
  - BeatStepTests/BPMViewWiringTests.swift
  - BeatStepTests/LibraryScanServiceTests.swift
autonomous: true
requirements: [CONF-01, CONF-02]

must_haves:
  truths:
    - "All existing test callers of cache() are updated to cacheFromAPI() and pass"
    - "cacheFromAPI sets .verified confidence and .api source for non-nil bpm"
    - "cacheManual sets .manual confidence and .manual source"
    - "cacheFromAPI preserves manual BPM (no-op for bpm when .manual confidence exists)"
    - "cacheManual overwrites API-sourced BPM"
    - "Lazy backfill returns .verified/.api for old records with bpm but no confidenceRaw"
    - "Nil bpm records return nil confidence and nil source"
  artifacts:
    - path: "BeatStepTests/BPMCacheServiceTests.swift"
      provides: "Updated existing tests + 7 new confidence/source tests"
      min_lines: 120
    - path: "BeatStepTests/BPMViewWiringTests.swift"
      provides: "Updated cache() calls to cacheFromAPI()"
      contains: "cacheFromAPI"
    - path: "BeatStepTests/LibraryScanServiceTests.swift"
      provides: "Updated cache() calls to cacheFromAPI()"
      contains: "cacheFromAPI"
  key_links:
    - from: "BeatStepTests/BPMCacheServiceTests.swift"
      to: "BeatStep/Services/BPMCacheService.swift"
      via: "Tests exercise cacheFromAPI and cacheManual write paths"
      pattern: "cacheFromAPI|cacheManual"
    - from: "BeatStepTests/BPMCacheServiceTests.swift"
      to: "BeatStep/Models/CachedBPM.swift"
      via: "Tests verify confidence/source computed properties and lazy backfill"
      pattern: "\\.confidence|confidenceRaw"
---

<objective>
Update all test files to use the new write paths and add comprehensive tests for confidence/source tracking and lazy backfill behavior.

Purpose: Verify that the data model changes from Plan 01 work correctly -- confidence is tracked, lazy backfill returns correct defaults, and the manual-wins-over-API guard functions as designed.
Output: Full green test suite validating all CONF-01 and CONF-02 behaviors.
</objective>

<execution_context>
@/Users/stijngragt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stijngragt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-bpm-confidence-model/18-CONTEXT.md
@.planning/phases/18-bpm-confidence-model/18-RESEARCH.md
@.planning/phases/18-bpm-confidence-model/18-01-SUMMARY.md

@BeatStepTests/BPMCacheServiceTests.swift
@BeatStepTests/BPMViewWiringTests.swift
@BeatStepTests/LibraryScanServiceTests.swift

<interfaces>
<!-- From Plan 01 outputs (new methods and types) -->

From BeatStep/Models/BPMConfidence.swift:
```swift
enum BPMConfidence: String, CaseIterable {
    case verified = "verified"
    case approximate = "approximate"
    case manual = "manual"
}
```

From BeatStep/Models/BPMSource.swift:
```swift
enum BPMSource: String, CaseIterable {
    case api = "api"
    case manual = "manual"
}
```

From BeatStep/Models/CachedBPM.swift (after Plan 01):
```swift
@Model
final class CachedBPM {
    @Attribute(.unique) var spotifyTrackID: String
    var trackName: String
    var artistName: String
    var bpm: Int?
    var lookupAttempted: Bool
    var lastUpdated: Date
    var danceability: Int?
    var confidenceRaw: String?
    var sourceRaw: String?

    var confidence: BPMConfidence? { get set }  // lazy backfill: nil raw + non-nil bpm -> .verified
    var source: BPMSource? { get set }          // lazy backfill: nil raw + non-nil bpm -> .api
    var isManual: Bool { get }
    var isVerified: Bool { get }
}
```

From BeatStep/Services/BPMCacheService.swift (after Plan 01):
```swift
func cacheFromAPI(trackID: String, name: String, artist: String, bpm: Int?)
func cacheManual(trackID: String, name: String, artist: String, bpm: Int)  // non-optional bpm
```

Existing test call sites to update (cache -> cacheFromAPI):
- BPMCacheServiceTests.swift: 8 calls (lines 28, 41, 42, 54, 61, 76, 77, 78)
- BPMViewWiringTests.swift: 7 calls (lines 28, 37, 38, 39, 47, 61, 74)
- LibraryScanServiceTests.swift: 3 calls (lines 31, 88, 89)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update existing test callers from cache() to cacheFromAPI()</name>
  <files>BeatStepTests/BPMCacheServiceTests.swift, BeatStepTests/BPMViewWiringTests.swift, BeatStepTests/LibraryScanServiceTests.swift</files>
  <action>
Rename all existing `service.cache(` calls to `service.cacheFromAPI(` across the three test files. This is a mechanical find-and-replace -- the parameter names and values remain identical.

**BPMCacheServiceTests.swift** (8 occurrences):
- Lines 28, 41, 42, 54, 61, 76, 77, 78: `service.cache(` -> `service.cacheFromAPI(`

**BPMViewWiringTests.swift** (7 occurrences):
- Lines 28, 37, 38, 39, 47, 61, 74: `service.cache(` -> `service.cacheFromAPI(`

**LibraryScanServiceTests.swift** (3 occurrences):
- Lines 31, 88, 89: `cacheService.cache(` -> `cacheService.cacheFromAPI(`

After renaming, run the full test suite to confirm all existing tests still pass with the renamed method.
  </action>
  <verify>
    <automated>xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20</automated>
  </verify>
  <done>All 18 cache() call sites across 3 test files renamed to cacheFromAPI(). Full test suite passes green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add confidence and source tracking tests</name>
  <files>BeatStepTests/BPMCacheServiceTests.swift</files>
  <behavior>
    - Test: cacheFromAPI with non-nil bpm sets confidence to .verified and source to .api
    - Test: cacheFromAPI with nil bpm sets confidence to nil and source to nil
    - Test: cacheManual sets confidence to .manual and source to .manual
    - Test: cacheFromAPI skips BPM overwrite when existing record has .manual confidence (but updates lookupAttempted and lastUpdated)
    - Test: cacheManual overwrites API-sourced BPM (replaces .verified with .manual)
    - Test: lazy backfill -- record created with no confidenceRaw but non-nil bpm returns .verified confidence and .api source
    - Test: lazy backfill -- record with nil bpm returns nil confidence and nil source
  </behavior>
  <action>
Add 7 new test methods to `BPMCacheServiceTests.swift` in a new `// MARK: - Confidence & Source Tracking` section:

1. `testCacheFromAPISetsVerifiedConfidence()`:
   - Call `cacheFromAPI(trackID: "t1", name: "Song", artist: "A", bpm: 120)`
   - Fetch the CachedBPM record directly from context
   - Assert `cached.confidence == .verified` and `cached.source == .api`
   - Assert `cached.confidenceRaw == "verified"` and `cached.sourceRaw == "api"`

2. `testCacheFromAPIWithNilBPMSetsNilConfidence()`:
   - Call `cacheFromAPI(trackID: "t1", name: "Song", artist: "A", bpm: nil)`
   - Fetch record, assert `cached.confidence == nil` and `cached.source == nil`
   - Assert `cached.confidenceRaw == nil` and `cached.sourceRaw == nil`

3. `testCacheManualSetsManualConfidence()`:
   - Call `cacheManual(trackID: "t1", name: "Song", artist: "A", bpm: 150)`
   - Fetch record, assert `cached.confidence == .manual` and `cached.source == .manual`
   - Assert `cached.bpm == 150`

4. `testCacheFromAPISkipsManualBPM()`:
   - Call `cacheManual(trackID: "t1", name: "Song", artist: "A", bpm: 150)` first
   - Then call `cacheFromAPI(trackID: "t1", name: "Song", artist: "A", bpm: 120)`
   - Fetch record, assert `cached.bpm == 150` (manual BPM preserved, not overwritten to 120)
   - Assert `cached.confidence == .manual` (still manual)
   - Assert `cached.lookupAttempted == true` (updated by cacheFromAPI)

5. `testCacheManualOverwritesAPIBPM()`:
   - Call `cacheFromAPI(trackID: "t1", name: "Song", artist: "A", bpm: 120)` first
   - Then call `cacheManual(trackID: "t1", name: "Song", artist: "A", bpm: 160)`
   - Fetch record, assert `cached.bpm == 160` and `cached.confidence == .manual`

6. `testLazyBackfillReturnsVerifiedForOldRecords()`:
   - Create a CachedBPM directly (simulating pre-migration record): `CachedBPM(spotifyTrackID: "old", trackName: "Old Song", artistName: "A", bpm: 130, lookupAttempted: true)`
   - Insert into context (do NOT set confidenceRaw or sourceRaw)
   - Assert `cached.confidence == .verified` and `cached.source == .api` (lazy backfill)
   - Assert `cached.confidenceRaw == nil` (raw field still nil -- backfill is in computed property only)

7. `testLazyBackfillReturnsNilForNilBPM()`:
   - Create CachedBPM directly with bpm: nil, no confidenceRaw
   - Assert `cached.confidence == nil` and `cached.source == nil`

Use the same setUp/tearDown pattern (in-memory ModelContainer) already established in the test file. To fetch records for assertion, use FetchDescriptor with #Predicate on spotifyTrackID, fetching from `container.mainContext`.
  </action>
  <verify>
    <automated>xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:BeatStepTests/BPMCacheServiceTests 2>&1 | tail -20</automated>
  </verify>
  <done>7 new test methods pass. All tests in BPMCacheServiceTests green. Confidence tracking, source tracking, manual-wins guard, and lazy backfill all verified.</done>
</task>

</tasks>

<verification>
- Full test suite passes: `xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -40`
- No remaining references to old `cache()` method in test files: `grep -rn '\.cache(' BeatStepTests/` returns zero results
- New confidence tests cover all 7 behaviors from the test map in RESEARCH.md
</verification>

<success_criteria>
1. All 18 existing cache() call sites renamed to cacheFromAPI() across 3 test files
2. 7 new test methods verify confidence/source tracking, manual-wins guard, and lazy backfill
3. Full test suite passes green (existing behavior preserved + new behavior validated)
4. Zero references to removed cache() method remain in test code
</success_criteria>

<output>
After completion, create `.planning/phases/18-bpm-confidence-model/18-02-SUMMARY.md`
</output>
