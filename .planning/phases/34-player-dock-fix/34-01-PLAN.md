---
phase: 34-player-dock-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BeatStep/App/ContentView.swift
  - BeatStep/Views/Player/MiniPlayerView.swift
autonomous: false
requirements:
  - PLAY-01
must_haves:
  truths:
    - "Mini player bottom edge is flush with tab bar top edge -- zero gap, zero overlap"
    - "Tab bar items remain fully tappable when mini player is visible"
    - "Scrollable content in Library and Settings does not clip behind mini player"
    - "When no track is playing, content extends down to tab bar with no reserved space"
    - "Player show/hide animates smoothly via BSAnimation.smooth"
  artifacts:
    - path: "BeatStep/App/ContentView.swift"
      provides: "TabView with correctly positioned .safeAreaInset MiniPlayerView"
      contains: ".safeAreaInset(edge: .bottom)"
    - path: "BeatStep/Views/Player/MiniPlayerView.swift"
      provides: "Self-contained player bar with correct background and shadow"
      contains: ".ultraThinMaterial"
  key_links:
    - from: "BeatStep/App/ContentView.swift"
      to: "MiniPlayerView"
      via: ".safeAreaInset(edge: .bottom) conditional on currentTrack"
      pattern: "safeAreaInset.*bottom.*MiniPlayerView"
---

<objective>
Fix mini player vertical positioning so it docks flush above the tab bar with zero gap and zero overlap, while correctly insetting scroll content.

Purpose: The mini player currently has positioning issues (overlap or gap with tab bar). This phase makes the player-tab-bar region feel like a unified dock (both use .ultraThinMaterial).

Output: Updated ContentView.swift and MiniPlayerView.swift with correct layout pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-player-dock-fix/34-CONTEXT.md
@.planning/phases/34-player-dock-fix/34-UI-SPEC.md

@BeatStep/App/ContentView.swift
@BeatStep/Views/Player/MiniPlayerView.swift
@BeatStep/DesignSystem/DesignTokens.swift

<interfaces>
<!-- Key types and contracts the executor needs -->

From BeatStep/App/ContentView.swift (lines 62-98):
```swift
private var authenticatedView: some View {
    TabView(selection: $selectedTab) {
        // 3 tabs: library, run, settings -- each wrapped in NavigationStack
    }
    .tint(Color.accent)
    .safeAreaInset(edge: .bottom) {
        if SpotifyPlayerService.shared.currentTrack != nil && !RunEngineService.shared.isRunActive {
            MiniPlayerView()
        }
    }
    .task { ... }
}
```

From BeatStep/Views/Player/MiniPlayerView.swift (lines 76-82):
```swift
.padding(.horizontal, Spacing.md)
.padding(.vertical, Spacing.sm)
.background(
    Rectangle()
        .fill(.ultraThinMaterial)
        .shadow(color: .black.opacity(0.1), radius: 4, y: -2)
)
```

Design tokens (from DesignTokens.swift):
- `Spacing.sm` = 8pt (MiniPlayer vertical padding)
- `Spacing.md` = 16pt (MiniPlayer horizontal padding)
- `BSAnimation.smooth` = standard animation timing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix player dock layout in ContentView and MiniPlayerView</name>
  <files>BeatStep/App/ContentView.swift, BeatStep/Views/Player/MiniPlayerView.swift</files>
  <read_first>
    - BeatStep/App/ContentView.swift (current layout composition)
    - BeatStep/Views/Player/MiniPlayerView.swift (current styling and padding)
    - BeatStep/DesignSystem/DesignTokens.swift (spacing tokens, BSAnimation)
    - .planning/phases/34-player-dock-fix/34-CONTEXT.md (locked decisions D-01 through D-04)
    - .planning/phases/34-player-dock-fix/34-UI-SPEC.md (layout contract)
  </read_first>
  <action>
Debug and fix the `.safeAreaInset(edge: .bottom)` layout pipeline in ContentView.swift so MiniPlayerView docks flush above the tab bar per D-01.

**Root cause investigation:**
The current code places `.safeAreaInset(edge: .bottom)` on the TabView (line 90). This is the correct SwiftUI pattern per D-03. Investigate whether:
1. The MiniPlayerView background Rectangle extends beyond its content (extra padding causing gap/overlap)
2. The `.safeAreaInset` content needs explicit sizing or alignment
3. NavigationStack wrapping inside each tab is interfering with inset propagation

**Fix approach in ContentView.swift (authenticatedView):**
- Keep `.safeAreaInset(edge: .bottom)` on the TabView per D-03
- Ensure the conditional `if SpotifyPlayerService.shared.currentTrack != nil` cleanly shows/hides the player
- Wrap the MiniPlayerView appearance in `.transition(.opacity)` with `BSAnimation.smooth` per D-04
- When player is hidden (no track or run active), ensure NO empty space is reserved -- content fills to tab bar per D-04

**Fix approach in MiniPlayerView.swift:**
- Keep the existing `.shadow(color: .black.opacity(0.1), radius: 4, y: -2)` per D-02
- Keep `.ultraThinMaterial` background per UI-SPEC material consistency contract
- Ensure the view does NOT add any extra bottom padding or spacing that would create a gap between its bottom edge and the tab bar top edge per D-01
- Verify that the `Group` wrapper (lines 8, 91) is not introducing unexpected layout behavior -- if it is, replace with a more explicit container or remove if unnecessary
- The view should be content-sized only: `Spacing.sm` (8pt) top and bottom padding + inner content = ~64pt total height per UI-SPEC

**Critical constraints:**
- Do NOT switch away from `.safeAreaInset` to manual padding (per D-03)
- Do NOT remove the top shadow (per D-02)
- Do NOT change MiniPlayerView internal content (BPM display, track info, controls) -- only layout/spacing
- Ensure tab bar items remain fully tappable with player visible (no z-order obstruction)
  </action>
  <verify>
    <automated>cd /Users/stijngragt/Projects/beatstep && xcodebuild -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16 Pro' build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ContentView.swift contains `.safeAreaInset(edge: .bottom)` on the TabView (grep confirms `safeAreaInset.*bottom`)
    - ContentView.swift conditional check includes both `currentTrack != nil` and `!RunEngineService.shared.isRunActive`
    - MiniPlayerView.swift contains `.ultraThinMaterial` background
    - MiniPlayerView.swift contains `shadow(color: .black.opacity(0.1), radius: 4, y: -2)`
    - MiniPlayerView.swift does NOT contain any bottom-only padding or spacer that would create gap below content (no `.padding(.bottom,` without matching `.padding(.top,`)
    - Project builds without errors
  </acceptance_criteria>
  <done>
    ContentView and MiniPlayerView compile successfully with the corrected layout pipeline. The .safeAreaInset approach is preserved, MiniPlayerView is content-sized with no extra bottom gap, shadow is retained, and material background is unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify player dock positioning on device</name>
  <files>BeatStep/App/ContentView.swift, BeatStep/Views/Player/MiniPlayerView.swift</files>
  <action>
Human verifies the visual result of the layout fix on simulator or device.

**What was built:** Fixed mini player layout so it docks flush above the tab bar with correct scroll content insetting.

**How to verify:**
1. Build and run the app in Simulator (iPhone 16 Pro or physical device)
2. Go to Library tab and play any track (or ensure Spotify is playing)
3. Check flush docking: Verify mini player bottom edge is directly touching tab bar top edge -- no visible gap, no overlap. Both should have matching blur material creating a unified dock look.
4. Check tab bar tappability: With player visible, tap Library, Run, and Settings tabs -- all must respond to taps normally.
5. Check scroll insetting: In Library tab, scroll to the bottom of the playlist list. Last item should be fully visible above the mini player, not clipped behind it.
6. Check player-hidden state: Stop Spotify playback (or start a run to hide player). Content should animate down to fill the space -- no ghost gap where the player was.
7. Check on iPhone SE simulator: Switch to iPhone SE (3rd gen) simulator and repeat steps 2-4. Player + tab bar should fit without compressing content.

**Resume signal:** Type "approved" if player docks correctly, or describe what is wrong (gap size, overlap, clipping, etc.)
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify -- requires manual visual inspection"</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms zero gap between mini player bottom edge and tab bar top edge
    - Human confirms all three tab bar items respond to taps with player visible
    - Human confirms last list item in Library is not clipped behind player
    - Human confirms no ghost space remains when player is hidden
  </acceptance_criteria>
  <done>Human confirms: mini player docks flush above tab bar, tabs are tappable, scroll content is not clipped, and player-hidden state reclaims space.</done>
</task>

</tasks>

<verification>
- Project builds without errors: `xcodebuild build` succeeds
- `.safeAreaInset(edge: .bottom)` pattern preserved in ContentView.swift
- MiniPlayerView shadow and material unchanged
- Visual verification on simulator confirms flush docking
</verification>

<success_criteria>
1. Mini player visually docks flush above tab bar on all tested screen sizes (SE, standard, Pro Max)
2. Tab bar items are fully tappable with player visible
3. Scroll content in Library/Settings does not clip behind player
4. Player show/hide animates without leaving reserved space
5. Human approves visual result in checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/34-player-dock-fix/34-01-SUMMARY.md`
</output>
