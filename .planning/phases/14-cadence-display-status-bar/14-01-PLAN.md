---
phase: 14-cadence-display-status-bar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BeatStep/Views/Run/RunStatusBar.swift
  - BeatStep/Views/Run/SyncBackgroundModifier.swift
  - BeatStep/Models/SyncQuality+Color.swift
  - BeatStepTests/CadenceDisplayTests.swift
autonomous: true
requirements: [RUN-03, CAD-04]

must_haves:
  truths:
    - "User sees zone name and sync quality badge in RunStatusBar"
    - "User perceives subtle background color shift based on sync state"
    - "SyncQuality maps to correct design token colors"
  artifacts:
    - path: "BeatStep/Models/SyncQuality+Color.swift"
      provides: "SyncQuality.color convenience extension"
      contains: "var color: Color"
    - path: "BeatStep/Views/Run/RunStatusBar.swift"
      provides: "RunStatusBar with zone name and SyncBadge"
      min_lines: 30
    - path: "BeatStep/Views/Run/SyncBackgroundModifier.swift"
      provides: "View modifier for sync-state background tint"
      contains: "SyncBackgroundModifier"
    - path: "BeatStepTests/CadenceDisplayTests.swift"
      provides: "Unit tests for color mapping and computation logic"
      min_lines: 20
  key_links:
    - from: "SyncQuality+Color.swift"
      to: "DesignTokens.swift"
      via: "Color.syncInSync/syncDrifting/syncMismatched tokens"
      pattern: "Color\\.sync"
    - from: "RunStatusBar.swift"
      to: "SyncQuality"
      via: "syncQuality parameter driving badge color"
      pattern: "SyncBadge.*quality"
---

<objective>
Build RunStatusBar (zone name + sync quality badge), SyncBackgroundModifier (subtle background color shift), and the SyncQuality.color bridge extension with unit tests for computation logic.

Purpose: Provide the glanceable status row and ambient sync feedback that runners see during active runs.
Output: RunStatusBar.swift, SyncBadge (inline), SyncBackgroundModifier.swift, SyncQuality+Color.swift, CadenceDisplayTests.swift
</objective>

<execution_context>
@/Users/stijngragt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stijngragt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-cadence-display-status-bar/14-RESEARCH.md
@.planning/phases/13-engine-extensions-design-tokens/13-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From BeatStep/Models/SyncQuality.swift:
```swift
enum SyncQuality: String, CaseIterable {
    case inSync
    case drifting
    case mismatched

    static func from(delta: Int, tolerance: BPMTolerance) -> SyncQuality
    var displayLabel: String  // "In Sync", "Drifting", "Mismatched"
}
```

From BeatStep/Models/RunZone.swift:
```swift
struct RunZone: Identifiable, Equatable {
    let id: Int
    let name: String
    var bpm: Int
    var displayLabel: String  // "Z1 Recovery", "Z3 Tempo", etc.
}
```

From BeatStep/DesignSystem/DesignTokens.swift (sync color tokens):
```swift
static let syncInSync = Color.stateSuccess
static let syncDrifting = Color.stateWarning
static let syncMismatched = Color.stateError
```

From BeatStep/DesignSystem/DesignTokens.swift (spacing/font tokens):
```swift
enum Spacing { static let xxs, xs, sm, md, lg, xl ... }
Font: .captionBold, .labelText, .bodyText, .displaySPM, .displaySecondary
Color: .textPrimary, .textSecondary, .surfaceBase, .surfaceOverlay, .accent
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SyncQuality.color extension and computation tests</name>
  <files>BeatStep/Models/SyncQuality+Color.swift, BeatStepTests/CadenceDisplayTests.swift</files>
  <behavior>
    - SyncQuality.inSync.color returns Color.syncInSync
    - SyncQuality.drifting.color returns Color.syncDrifting
    - SyncQuality.mismatched.color returns Color.syncMismatched
    - All three SyncQuality cases have non-nil color values
  </behavior>
  <action>
    Create `BeatStep/Models/SyncQuality+Color.swift` with `import SwiftUI` and a `SyncQuality` extension that adds a computed `color: Color` property mapping each case to the corresponding DesignTokens sync color (Color.syncInSync, Color.syncDrifting, Color.syncMismatched). Per research recommendation, `import SwiftUI` in the extension file is the simpler approach (iOS-only project).

    Create `BeatStepTests/CadenceDisplayTests.swift` as the test scaffold for Phase 14. Start with tests for SyncQuality.color mapping (verify each case returns the expected token color). This test file will also hold computation tests for ZoneBandView position and RampPhaseIndicator progress in Plan 02.

    Register both new files in the Xcode project by running `xcodegen` after creation.
  </action>
  <verify>
    <automated>cd /Users/stijngragt/Projects/beatstep && DEVELOPER_DIR=/Applications/Xcode.app xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:BeatStepTests/CadenceDisplayTests 2>&1 | tail -20</automated>
  </verify>
  <done>SyncQuality.color extension exists, maps to correct design tokens, tests pass</done>
</task>

<task type="auto">
  <name>Task 2: RunStatusBar, SyncBadge, and SyncBackgroundModifier views</name>
  <files>BeatStep/Views/Run/RunStatusBar.swift, BeatStep/Views/Run/SyncBackgroundModifier.swift</files>
  <action>
    **RunStatusBar.swift** -- Create a pure SwiftUI view taking explicit parameters (not reading RunEngineService.shared):
    - Parameters: `zoneName: String?` (nil in free mode), `syncQuality: SyncQuality`
    - Layout: HStack with zone name on left (if non-nil), Spacer, SyncBadge on right
    - Use `Font.captionBold` for zone name, `Color.textPrimary` foreground
    - Padding: `.horizontal, Spacing.md` and `.vertical, Spacing.sm`

    **SyncBadge** (private struct inside RunStatusBar.swift):
    - Parameters: `quality: SyncQuality`
    - Layout: Text(quality.displayLabel) in a pill/capsule shape
    - Font: `.labelText`, foreground: `quality.color`
    - Background: `Capsule().fill(quality.color.opacity(0.15))`
    - Padding: `.horizontal, Spacing.sm` and `.vertical, Spacing.xxs`
    - Animation: `.easeInOut(duration: 0.3)` on `value: quality`

    **SyncBackgroundModifier.swift** -- Create a ViewModifier:
    - Parameters: `syncQuality: SyncQuality`
    - Computes backgroundColor from `syncQuality.color`
    - Applies as background with `opacity(0.08)` (subtle, per research -- must be subconscious)
    - `.ignoresSafeArea()` so it fills the full screen
    - `.animation(.easeInOut(duration: 0.6), value: syncQuality)` for smooth transitions
    - Add `View` extension: `func syncBackground(_ quality: SyncQuality) -> some View`

    **Previews** -- Add #Preview blocks to both files:
    - RunStatusBar: "In Sync" with zone name, "Free Mode" with nil zone, "Mismatched" state
    - SyncBackgroundModifier: Show a VStack with text content wrapped in `.syncBackground(.inSync)` and `.syncBackground(.drifting)`

    Run `xcodegen` to register new files, then build to verify compilation.
  </action>
  <verify>
    <automated>cd /Users/stijngragt/Projects/beatstep && DEVELOPER_DIR=/Applications/Xcode.app xcodebuild build -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 17 Pro' 2>&1 | tail -5</automated>
  </verify>
  <done>RunStatusBar shows zone name + colored sync badge, SyncBackgroundModifier applies subtle tinted background, all previews render without crashes, project builds clean</done>
</task>

</tasks>

<verification>
1. `xcodebuild test` passes all CadenceDisplayTests (color mapping)
2. `xcodebuild build` succeeds with zero errors
3. RunStatusBar accepts zoneName + syncQuality, renders badge with correct color
4. SyncBackgroundModifier applies barely-visible tint (opacity 0.08) per sync state
5. All existing tests still pass
</verification>

<success_criteria>
- RunStatusBar renders zone name and sync quality badge with correct design token colors (RUN-03)
- SyncBackgroundModifier applies subtle background color shift based on sync state (CAD-04)
- SyncQuality.color extension bridges model to design system
- All views are pure (take parameters, not singletons) and fully previewable
- CadenceDisplayTests test file created as scaffold for Phase 14 computation tests
</success_criteria>

<output>
After completion, create `.planning/phases/14-cadence-display-status-bar/14-01-SUMMARY.md`
</output>
