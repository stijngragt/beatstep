---
phase: 11-run-experience
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - BeatStep/Views/Run/RunView.swift
  - BeatStep/Models/PacePreset.swift
  - BeatStep/Views/Run/PacePresetPicker.swift
  - BeatStep/Views/Run/ModePicker.swift
  - BeatStepTests/PacePresetTests.swift
autonomous: true
requirements:
  - RUN-01

must_haves:
  truths:
    - "RunView idle state reads zone selection from RunZone.selectedZoneId instead of showing ModePicker/PacePresetPicker"
    - "RunView Start Run button sets runMode and targetBPM from persisted zone selection"
    - "PacePreset enum, PacePresetPicker, ModePicker, and PacePresetTests are deleted"
    - "Project builds and all tests pass with no references to deleted types"
  artifacts:
    - path: "BeatStep/Views/Run/RunView.swift"
      provides: "Updated run view using zone selection"
      contains: "RunZone.selectedZoneId"
  key_links:
    - from: "BeatStep/Views/Run/RunView.swift"
      to: "RunZone.selectedZoneId"
      via: "reads persisted zone for run mode determination"
      pattern: "RunZone\\.selectedZoneId"
    - from: "BeatStep/Views/Run/RunView.swift"
      to: "RunMode.savedTargetBPM"
      via: "sets target BPM from zone on run start"
      pattern: "RunMode\\.savedTargetBPM"
---

<objective>
Migrate RunView from PacePreset to zone selection and remove dead code.

Purpose: Complete the zone migration by updating RunView's idle state and start-run logic, then clean up all replaced components.
Output: RunView uses zone selection, all old preset/mode picker code deleted.
</objective>

<execution_context>
@/Users/stijngragt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stijngragt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-run-experience/11-CONTEXT.md
@.planning/phases/11-run-experience/11-RESEARCH.md
@.planning/phases/11-run-experience/11-01-SUMMARY.md

@BeatStep/Views/Run/RunView.swift
@BeatStep/Models/RunZone.swift
@BeatStep/Models/RunMode.swift
@BeatStep/Models/PacePreset.swift
@BeatStep/Views/Run/ModePicker.swift
@BeatStep/Views/Run/PacePresetPicker.swift

<interfaces>
<!-- After Plan 01 creates selectedZoneId on RunZone -->

From BeatStep/Models/RunZone.swift (after Plan 01):
```swift
extension RunZone {
    static var selectedZoneId: Int? { get set }  // nil = Free, 1-5 = zone
}
```

From BeatStep/Models/RunMode.swift:
```swift
enum RunMode: String, CaseIterable {
    case free, guided
    static var saved: RunMode
    func save()
    static var savedTargetBPM: Int { get set }
}
```

Current RunView state properties to replace:
```swift
// REMOVE these:
@State private var runMode: RunMode = .saved
@State private var selectedPreset: PacePreset = ...
@State private var customBPM: Int = RunMode.savedTargetBPM

// REPLACE with:
@State private var selectedZoneId: Int? = RunZone.selectedZoneId
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update RunView to use zone selection and remove old pickers</name>
  <files>BeatStep/Views/Run/RunView.swift</files>
  <action>
Modify RunView to read zone selection instead of using ModePicker/PacePresetPicker:

1. Replace state properties:
   - Remove: `@State private var runMode: RunMode`, `@State private var selectedPreset: PacePreset`, `@State private var customBPM: Int`
   - Add: `@State private var selectedZoneId: Int? = RunZone.selectedZoneId`
   - Keep: `@State private var tolerance: BPMTolerance = .saved`

2. Update `idleView`:
   - Remove `ModePicker(mode: $runMode)` and its padding
   - Remove the conditional `PacePresetPicker` block
   - Show selected zone info as read-only label instead:
     - If `selectedZoneId` is non-nil, find zone in `RunZone.saved` and show `Text(zone.displayLabel)` with `.font(.subheading)` and `.foregroundStyle(Color.textPrimary)` plus `Text("\(zone.bpm) BPM")` with `.font(.captionText)` and `.foregroundStyle(Color.textSecondary)`
     - If nil (Free), show `Text("Free Run")` with `.font(.subheading)` and `.foregroundStyle(Color.textPrimary)`
   - Keep "Ready to Run" text
   - Keep `TolerancePicker` but only show when selectedZoneId != nil (zone = guided mode)

3. Update `controlsSection` start button action:
   - Replace the old runMode/targetBPM logic with zone-based logic:
     ```swift
     if let zoneId = selectedZoneId,
        let zone = RunZone.saved.first(where: { $0.id == zoneId }) {
         runEngine.runMode = .guided
         runEngine.tolerance = tolerance
         RunMode.savedTargetBPM = zone.bpm
     } else {
         runEngine.runMode = .free
         runEngine.tolerance = tolerance
     }
     ```
   - Keep all other start-run logic (LastRunPlaylist save, cadence start, idle timer, engine start)

4. Update `targetBPM` computed property:
   - Replace with: look up zone from selectedZoneId in RunZone.saved, return zone.bpm or RunMode.savedTargetBPM as fallback

5. Remove all imports/references to PacePreset. Ensure no ModePicker or PacePresetPicker references remain.
  </action>
  <verify>
    <automated>xcodebuild build -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -10</automated>
  </verify>
  <done>RunView compiles without ModePicker, PacePresetPicker, or PacePreset references; idle state shows zone info read-only; start button sets runMode from zone selection</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead code and clean up tests</name>
  <files>BeatStep/Models/PacePreset.swift, BeatStep/Views/Run/PacePresetPicker.swift, BeatStep/Views/Run/ModePicker.swift, BeatStepTests/PacePresetTests.swift</files>
  <action>
1. Delete these files entirely:
   - `BeatStep/Models/PacePreset.swift`
   - `BeatStep/Views/Run/PacePresetPicker.swift`
   - `BeatStep/Views/Run/ModePicker.swift`
   - `BeatStepTests/PacePresetTests.swift`

2. Verify no remaining references to deleted types anywhere in the project:
   - Search for "PacePreset", "PacePresetPicker", "ModePicker" across all .swift files
   - If any references found, update those files to remove the references
   - Common places to check: RunView.swift (handled in Task 1), any preview providers

3. Build to confirm clean compilation with no references to deleted types.

4. Run full test suite to confirm nothing breaks:
   - RunZoneTests should still pass
   - ZoneSelectionTests (from Plan 01) should still pass
   - No test references deleted types
  </action>
  <verify>
    <automated>xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -30</automated>
  </verify>
  <done>PacePreset.swift, PacePresetPicker.swift, ModePicker.swift, PacePresetTests.swift deleted; project builds with zero references to deleted types; full test suite green</done>
</task>

</tasks>

<verification>
1. Build succeeds: `xcodebuild build -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16'`
2. Full test suite passes: `xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16'`
3. No references to deleted types: `grep -r "PacePreset\|ModePicker" BeatStep/ BeatStepTests/ --include="*.swift"` returns empty
</verification>

<success_criteria>
- RunView idle state shows selected zone label (read-only) instead of ModePicker + PacePresetPicker
- RunView start button derives runMode and targetBPM from zone selection
- PacePreset enum, PacePresetPicker, ModePicker, and PacePresetTests are fully removed
- Full test suite passes with zero dead-code references
</success_criteria>

<output>
After completion, create `.planning/phases/11-run-experience/11-02-SUMMARY.md`
</output>
