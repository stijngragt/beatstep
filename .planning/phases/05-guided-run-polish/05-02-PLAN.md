---
phase: 05-guided-run-polish
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - BeatStep/Views/Run/RunView.swift
  - BeatStep/Views/Run/ModePicker.swift
  - BeatStep/Views/Run/PacePresetPicker.swift
  - BeatStep/Views/Run/TolerancePicker.swift
  - BeatStep.xcodeproj/project.pbxproj
autonomous: false
requirements: [RUN-02, RUN-03]

must_haves:
  truths:
    - "User can switch between Free and Guided mode before starting a run"
    - "Selecting Guided reveals target BPM configuration with pace presets"
    - "Custom preset opens a numeric picker for arbitrary BPM"
    - "During guided run, phase label shows 'Warming up' / 'At pace' / 'Cooling down'"
    - "Cool Down button appears during guided active run"
    - "Tapping Cool Down triggers ramp-down to warm-up BPM"
    - "Last-used mode and target BPM persist between runs"
  artifacts:
    - path: "BeatStep/Views/Run/ModePicker.swift"
      provides: "Free/Guided segmented control"
      contains: "ModePicker"
    - path: "BeatStep/Views/Run/PacePresetPicker.swift"
      provides: "Named pace preset picker with Custom option"
      contains: "PacePresetPicker"
    - path: "BeatStep/Views/Run/RunView.swift"
      provides: "Extended with mode picker, pace config, phase labels, cool down button"
      contains: "ModePicker"
  key_links:
    - from: "BeatStep/Views/Run/RunView.swift"
      to: "BeatStep/Services/RunEngineService.swift"
      via: "startRun with mode parameter, reads rampPhase for label"
      pattern: "runEngine\\.rampPhase"
    - from: "BeatStep/Views/Run/RunView.swift"
      to: "BeatStep/Models/RunMode.swift"
      via: "mode state drives UI visibility"
      pattern: "RunMode"
    - from: "BeatStep/Views/Run/RunView.swift"
      to: "BeatStep/Services/RunEngineService.swift"
      via: "Cool Down button calls startCoolDown()"
      pattern: "startCoolDown"
---

<objective>
Wire guided run mode into RunView UI: mode picker (Free/Guided), target BPM preset selector, ramp phase labels during active run, and Cool Down button. This connects Plan 01's engine logic to the user-facing interface.

Purpose: Users need to configure and control guided runs. The engine logic exists from Plan 01; this plan makes it accessible.
Output: Complete guided run UI, verified on device.
</objective>

<execution_context>
@/Users/stijngragt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stijngragt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-guided-run-polish/05-CONTEXT.md
@.planning/phases/05-guided-run-polish/05-01-SUMMARY.md

@BeatStep/Views/Run/RunView.swift
@BeatStep/Views/Run/TolerancePicker.swift
@BeatStep/Services/RunEngineService.swift
@BeatStep/Models/RunMode.swift
@BeatStep/Models/RampPhase.swift
@BeatStep/Models/PacePreset.swift

<interfaces>
<!-- Contracts from Plan 01 that this plan wires into UI -->

From BeatStep/Models/RunMode.swift (created in Plan 01):
```swift
enum RunMode: String, CaseIterable {
    case free, guided
    static var saved: RunMode
    func save()
    static var savedTargetBPM: Int  // get/set, default 160
}
```

From BeatStep/Models/RampPhase.swift (created in Plan 01):
```swift
enum RampPhase: String {
    case warmUp, atPace, coolDown
    var displayLabel: String  // "Warming up" / "At pace" / "Cooling down"
}
```

From BeatStep/Models/PacePreset.swift (created in Plan 01):
```swift
enum PacePreset: String, CaseIterable, Identifiable {
    case easyJog, steady, tempo, fast, sprint, custom
    var bpm: Int?  // nil for custom
    var displayName: String
}
```

From BeatStep/Services/RunEngineService.swift (extended in Plan 01):
```swift
// New observable properties:
var runMode: RunMode
var rampPhase: RampPhase?  // nil in free mode

// New methods:
func startRun(playlist: SpotifyPlaylist, tracks: [SpotifyTrack], mode: RunMode) async
func startCoolDown()
```

From BeatStep/Views/Run/TolerancePicker.swift (existing):
```swift
struct TolerancePicker: View {
    @Binding var tolerance: BPMTolerance
    // Segmented control with Tight/Normal/Loose
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ModePicker, PacePresetPicker, and wire into RunView</name>
  <files>
    BeatStep/Views/Run/ModePicker.swift,
    BeatStep/Views/Run/PacePresetPicker.swift,
    BeatStep/Views/Run/RunView.swift,
    BeatStep.xcodeproj/project.pbxproj
  </files>
  <action>
    1. Create ModePicker.swift:
       - Segmented control (Picker with .segmented style) for RunMode: "Free" / "Guided"
       - `@Binding var mode: RunMode`
       - Dark theme styling consistent with TolerancePicker (white text, dark background)
       - On change: call mode.save() for UserDefaults persistence

    2. Create PacePresetPicker.swift:
       - Horizontal scrolling row of preset buttons OR a Picker with .menu/.wheel style
       - Shows PacePreset cases: "Easy Jog (150)", "Steady (160)", "Tempo (170)", "Fast (180)", "Sprint (190)", "Custom"
       - `@Binding var selectedPreset: PacePreset`
       - `@Binding var customBPM: Int` -- shown only when Custom selected (Stepper or Picker, range 120-200)
       - `var targetBPM: Int` computed: selectedPreset.bpm ?? customBPM
       - On change: persist target BPM to UserDefaults via RunMode.savedTargetBPM
       - Dark theme styling matching run screen aesthetic

    3. Update RunView idle state:
       - Add `@State private var runMode: RunMode = .saved`
       - Add `@State private var selectedPreset: PacePreset = .steady` (derive from savedTargetBPM if it matches a preset)
       - Add `@State private var customBPM: Int = RunMode.savedTargetBPM`
       - Show ModePicker below playlist name, above "Ready to Run"
       - When mode == .guided: show PacePresetPicker below ModePicker
       - TolerancePicker stays visible for BOTH modes (per user decision)

    4. Update RunView controls section:
       - Start Run button passes mode to engine: `runEngine.startRun(playlist:tracks:mode:)` where mode is `.free` or `.guided(targetBPM: targetBPM)`
       - Note: The RunMode enum is simple (.free/.guided), but startRun may accept mode with associated targetBPM -- adapt to whatever Plan 01 implemented

    5. Update RunView active state:
       - When `runEngine.rampPhase != nil`: show phase label (runEngine.rampPhase!.displayLabel) as subtle text
       - Label styled: `.font(.subheadline)`, `.foregroundStyle(.white.opacity(0.6))`, positioned above cadence display
       - Do NOT show target BPM number (user decision: phase labels only)

    6. Update RunView controls for guided active state:
       - During guided active run (runEngine.runMode == .guided && cadenceService.state != .idle):
         - Show "Cool Down" button alongside or replacing "Stop Run"
         - If rampPhase == .coolDown: show only "Stop Run" (cool-down already active)
         - If rampPhase == .warmUp or .atPace: show both "Cool Down" (orange capsule) and "Stop Run" (red capsule)
         - Cool Down button calls `runEngine.startCoolDown()`
         - Cool Down button styling: orange capsule, `.font(.headline)`, icon "arrow.down.heart"

    7. Run `xcodegen` to regenerate project file with new ModePicker.swift and PacePresetPicker.swift

    Commit: `feat(05-02): wire guided run UI with mode picker, pace presets, phase labels, and cool down`
  </action>
  <verify>
    <automated>xcodebuild build -project BeatStep.xcodeproj -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -10</automated>
    Build succeeds with no errors.
  </verify>
  <done>ModePicker and PacePresetPicker components created. RunView shows mode picker in idle state, pace presets when guided selected, phase labels during active guided run, and Cool Down button during guided run. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete guided run flow on device</name>
  <action>
    Human verifies the complete guided run experience on a physical device. This covers both the UI elements wired in this plan and the engine logic from Plan 01.
  </action>
  <what-built>
    Complete guided run mode UI and engine:
    - Free/Guided mode picker on run screen (idle state)
    - Named pace presets (Easy Jog through Sprint + Custom)
    - Warm-up/cool-down ramp with "Warming up" / "At pace" / "Cooling down" labels
    - Cool Down button during guided runs
    - Smart song selection by danceability (replaces random in both modes)
    - On-demand catalog discovery when song pool runs low
    - All engine logic TDD-tested (Plan 01)
  </what-built>
  <how-to-verify>
    1. Build and run BeatStep on device
    2. Navigate to a playlist with scanned BPM data -> tap "Run with this Playlist"
    3. Verify idle state:
       a. Free/Guided segmented control visible
       b. Select "Guided" -- pace preset picker appears
       c. Select different presets, verify Custom shows numeric input
       d. Tolerance picker visible for both modes
       e. Switch back to Free -- preset picker hides
    4. Start a Free Run:
       a. Verify music plays matched to cadence (same as Phase 4)
       b. Note: songs should feel slightly less random (smart selection active)
       c. Stop run
    5. Start a Guided Run:
       a. Select "Steady" preset (160 BPM)
       b. Tap Start Run
       c. Verify "Warming up" label appears subtly on run screen
       d. After a few songs, verify music BPM is stepping up toward 160
       e. When "At pace" label appears, verify songs are at ~160 BPM
       f. Tap "Cool Down" button
       g. Verify "Cooling down" label appears
       h. Songs should step back down in BPM
    6. Close and reopen run screen -- verify last mode (Guided) and target BPM persist
  </how-to-verify>
  <verify>Human approves complete guided run experience</verify>
  <done>Guided run flow verified on device: mode switching, pace presets, ramp progression, phase labels, cool down, and persistence all working correctly.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- Build succeeds: `xcodebuild build -project BeatStep.xcodeproj -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16'`
- All tests pass: `xcodebuild test -project BeatStep.xcodeproj -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:BeatStepTests 2>&1 | tail -40`
- Human verification of complete guided run flow on device
</verification>

<success_criteria>
- Free/Guided mode picker visible on RunView idle state
- Guided mode reveals pace preset picker with correct presets
- Custom preset allows arbitrary BPM input
- Phase labels ("Warming up" / "At pace" / "Cooling down") show during guided run
- Cool Down button triggers ramp-down
- Mode and target BPM persist between runs
- Smart selection active in both modes
- Human approves the complete flow on device
</success_criteria>

<output>
After completion, create `.planning/phases/05-guided-run-polish/05-02-SUMMARY.md`
</output>
