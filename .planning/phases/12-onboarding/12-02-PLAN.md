---
phase: 12-onboarding
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - BeatStep/Views/Settings/SettingsView.swift
  - BeatStepTests/OnboardingTests.swift
autonomous: false
requirements: [ONBD-04]

must_haves:
  truths:
    - "User who denied permissions can find a Revisit Permissions section in Settings"
    - "Motion permission status is shown accurately (Granted vs Check Settings)"
    - "Health permission shows Requested or Not Yet (cannot detect denied for read types)"
    - "Open Settings button launches iOS Settings app for the BeatStep app"
  artifacts:
    - path: "BeatStep/Views/Settings/SettingsView.swift"
      provides: "Permissions section with status display and Open Settings link"
      contains: "openSettingsURLString"
  key_links:
    - from: "BeatStep/Views/Settings/SettingsView.swift"
      to: "iOS Settings"
      via: "UIApplication.shared.open(UIApplication.openSettingsURLString)"
      pattern: "openSettingsURLString"
    - from: "BeatStep/Views/Settings/SettingsView.swift"
      to: "@AppStorage flags"
      via: "hasRequestedHealth and hasRequestedMotion flags from onboarding"
      pattern: "hasRequestedHealth|hasRequestedMotion"
---

<objective>
Add a Permissions section to SettingsView so users who denied permissions during onboarding can recover by navigating to iOS Settings.

Purpose: Users who denied Spotify, Motion, or Health permissions on first launch need a way to restore access without reinstalling.
Output: Updated SettingsView with Permissions section, updated tests.
</objective>

<execution_context>
@/Users/stijngragt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stijngragt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-onboarding/12-RESEARCH.md
@.planning/phases/12-onboarding/12-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — @AppStorage flags created during onboarding -->
<!-- These flags are set by OnboardingHealthView and available via @AppStorage -->
@AppStorage("hasRequestedHealth") — set to true after HealthKit requestAuthorization
@AppStorage("hasRequestedMotion") — set to true after CadenceService.requestPermissionAndStart()
@AppStorage("hasCompletedOnboarding") — set to true when onboarding completes

From BeatStep/Views/Settings/SettingsView.swift (current):
```swift
struct SettingsView: View {
    private var authService: SpotifyAuthService { .shared }
    @State private var zones: [RunZone] = RunZone.saved
    // Sections: Account, Running Zones, Disconnect
    // Must add: Permissions section between Running Zones and Disconnect
}
```

From BeatStep/Services/CadenceService.swift:
```swift
CMPedometer.authorizationStatus() // returns .authorized, .restricted, .denied, .notDetermined
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Permissions section to SettingsView</name>
  <files>BeatStep/Views/Settings/SettingsView.swift, BeatStepTests/OnboardingTests.swift</files>
  <action>
    1. Update `SettingsView.swift`:
       - Add `import HealthKit` and `import CoreMotion`
       - Add `@AppStorage("hasRequestedHealth") private var hasRequestedHealth = false`
       - Add `@AppStorage("hasRequestedMotion") private var hasRequestedMotion = false`

    2. Add a "Permissions" Section between "Running Zones" and the Disconnect section:
       - **Motion Access row:** HStack with "Motion Access" label (textSecondary) and status text. Status: if `CMPedometer.authorizationStatus() == .authorized` show "Granted" in stateSuccess color, else show "Check Settings" in stateWarning color. Use `.font(.captionText)`.

       - **Apple Health row (conditional):** Only show if `HKHealthStore.isHealthDataAvailable()`. HStack with "Apple Health" label (textSecondary) and status. Status: if `hasRequestedHealth` show "Requested" else "Not Yet", both in textSecondary color with `.font(.captionText)`. Note: do NOT check `authorizationStatus` for read types -- it always returns `.notDetermined` for denied.

       - **Open Settings button:** `Button("Open Settings")` that opens `UIApplication.openSettingsURLString`. Style with `Color.accent` foreground.

    3. Add test to `OnboardingTests.swift`:
       - Test that the URL string `UIApplication.openSettingsURLString` is non-empty (sanity check)
       - Test AppStorage flags default to false

    4. Run xcodegen and build.
  </action>
  <verify>
    <automated>cd /Users/stijngragt/Projects/beatstep && xcodegen generate 2>&1 | tail -3 && xcodebuild build -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20</automated>
  </verify>
  <done>SettingsView shows Permissions section with Motion status, Health status (on supported devices), and Open Settings button. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete onboarding flow end-to-end</name>
  <files>N/A</files>
  <action>
    Human verifies the complete onboarding flow on device/simulator. This checkpoint covers all of Plan 01 + Plan 02 work together as the final v1.2 feature.
  </action>
  <verify>
    <automated>cd /Users/stijngragt/Projects/beatstep && xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -20</automated>
  </verify>
  <done>User has verified the onboarding flow works correctly end-to-end and approved.</done>
  <what-built>Complete onboarding flow: 3-screen first-launch experience (Spotify, Health/Motion, Zones) with Settings permission recovery. This is the final feature of v1.2.</what-built>
  <how-to-verify>
    1. Delete the app from Simulator to simulate fresh install
    2. Build and run the app
    3. Verify first screen shows "Your Music Library" value frame with Spotify connect button (NOT the old LoginView)
    4. Tap "Connect with Spotify" -- verify system OAuth dialog appears AFTER the value frame
    5. After Spotify auth, verify second screen shows "Motion & Health" value frame
    6. Tap "Allow Permissions" or "Skip" -- verify transition to zones screen
    7. Verify zones screen shows Z1-Z5 overview with "Get Started" button
    8. Tap "Get Started" -- verify app transitions to TabView (authenticated) or LoginView
    9. Navigate to Settings tab -> verify "Permissions" section shows Motion status and Open Settings button
    10. Tap "Open Settings" -- verify iOS Settings opens to BeatStep page
    11. Verify library scan did NOT fire during onboarding (no network activity in console before completing onboarding)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Build succeeds: `xcodebuild build -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16'`
2. Tests pass: `xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:BeatStepTests/OnboardingTests`
3. Full test suite: `xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16'`
</verification>

<success_criteria>
- Settings shows Permissions section with Motion and Health status
- Open Settings button launches iOS Settings for BeatStep
- Motion shows "Granted" or "Check Settings" accurately
- Health shows "Requested" or "Not Yet" (never claims granted/denied)
- Full onboarding flow verified by user on device/simulator
</success_criteria>

<output>
After completion, create `.planning/phases/12-onboarding/12-02-SUMMARY.md`
</output>
