---
phase: 28-library-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BeatStepTests/PlaylistFilterTests.swift
  - BeatStepTests/PlaylistCoverageTests.swift
  - BeatStepTests/LibraryScanServiceTests.swift
  - BeatStep/Views/Library/PlaylistListView.swift
  - BeatStep/Services/LibraryScanService.swift
  - BeatStep/DesignSystem/DesignTokens.swift
autonomous: true
requirements: [LIB-01, LIB-02, LIB-03, LIB-04]

must_haves:
  truths:
    - "PlaylistCoverage struct provides numeric coverage data for bar rendering"
    - "coverageData dictionary replaces coverageMap with rich typed data"
    - "deleteScan removes ScannedPlaylist record from SwiftData"
    - "coverArtMedium token (56pt) available in ComponentSize"
    - "PlaylistFilter enum enables All/Analyzed/Unanalyzed filtering"
    - "filteredPlaylists computed property applies search + filter compound logic"
  artifacts:
    - path: "BeatStepTests/PlaylistFilterTests.swift"
      provides: "Unit test stubs for PlaylistFilter and filteredPlaylists"
      contains: "class PlaylistFilterTests"
    - path: "BeatStepTests/PlaylistCoverageTests.swift"
      provides: "Unit test stubs for PlaylistCoverage struct"
      contains: "class PlaylistCoverageTests"
    - path: "BeatStepTests/LibraryScanServiceTests.swift"
      provides: "testDeleteScan stub added to existing test file"
      contains: "testDeleteScan"
    - path: "BeatStep/Views/Library/PlaylistListView.swift"
      provides: "PlaylistCoverage struct, PlaylistFilter enum, filteredPlaylists, coverageData dictionary"
      contains: "struct PlaylistCoverage"
    - path: "BeatStep/Services/LibraryScanService.swift"
      provides: "deleteScan method"
      contains: "func deleteScan"
    - path: "BeatStep/DesignSystem/DesignTokens.swift"
      provides: "coverArtMedium size token"
      contains: "coverArtMedium"
  key_links:
    - from: "PlaylistListView.swift"
      to: "LibraryScanService.swift"
      via: "deleteScan call from context menu"
      pattern: "scanService\\.deleteScan"
    - from: "PlaylistCoverage.statusColor"
      to: "Color.stateSuccess/stateWarning/stateError"
      via: "threshold-based color"
      pattern: "stateSuccess|stateWarning|stateError"
---

<objective>
Add data model, filtering logic, scan deletion, and design tokens needed for the Library Polish UI -- with Wave 0 test stubs created first.

Purpose: The UI plan (28-02) depends on PlaylistCoverage struct, PlaylistFilter enum, filteredPlaylists computed property, deleteScan service method, and coverArtMedium token. Building these first gives the UI plan clean contracts to consume. Wave 0 test stubs enable Nyquist-compliant automated verification throughout.
Output: Test stubs (PlaylistFilterTests, PlaylistCoverageTests, testDeleteScan), updated PlaylistListView.swift (data model + filtering logic), LibraryScanService.swift (deleteScan), DesignTokens.swift (coverArtMedium)
</objective>

<execution_context>
@/Users/stijngragt/.claude/get-shit-done/workflows/execute-plan.md
@/Users/stijngragt/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-library-polish/28-CONTEXT.md
@.planning/phases/28-library-polish/28-RESEARCH.md

@BeatStep/Views/Library/PlaylistListView.swift
@BeatStep/Services/LibraryScanService.swift
@BeatStep/DesignSystem/DesignTokens.swift
@BeatStep/DesignSystem/BSHaptics.swift
@BeatStep/DesignSystem/BSAnimation.swift
@BeatStepTests/LibraryScanServiceTests.swift

<interfaces>
<!-- Existing types used by this plan -->

From BeatStep/DesignSystem/DesignTokens.swift:
```swift
enum ComponentSize {
    static let miniPlayerHeight: CGFloat = 64
    static let buttonHeight: CGFloat = 52
    static let coverArtSmall: CGFloat = 44
    static let coverArtLarge: CGFloat = 200
    static let iconSmall: CGFloat = 24
    static let iconMedium: CGFloat = 44
    static let iconLarge: CGFloat = 60
}
```

From BeatStep/Services/LibraryScanService.swift:
```swift
@MainActor @Observable
final class LibraryScanService {
    static let shared = LibraryScanService()
    var scanningPlaylistID: String?
    var scanProgress: ScanProgress?
    func scanPlaylistByID(_ playlistID: String, name: String) async
}
```

From BeatStep/Views/Library/PlaylistListView.swift:
```swift
@State private var coverageMap: [String: String] = [:]
@State private var coverageLoaded = false
// loadCoverageData() builds coverageMap from ScannedPlaylist SwiftData
```

From BeatStep/App/ContentView.swift (SelectedTabKey pattern):
```swift
@Environment(\.selectedTab) private var selectedTab
// selectedTab.wrappedValue = .run  // switches to Run tab
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Wave 0 test stubs for Nyquist compliance</name>
  <files>BeatStepTests/PlaylistFilterTests.swift, BeatStepTests/PlaylistCoverageTests.swift, BeatStepTests/LibraryScanServiceTests.swift</files>
  <action>
Create test stub files that will compile and fail (red) until implementation tasks complete them. These are the Nyquist Wave 0 requirements from VALIDATION.md.

**1. Create `BeatStepTests/PlaylistFilterTests.swift`** (covers LIB-01 + LIB-02):
```swift
import XCTest
@testable import BeatStep

final class PlaylistFilterTests: XCTestCase {

    // MARK: - PlaylistFilter Enum (LIB-02)

    func testFilterAllReturnsAllPlaylists() {
        // Stub: Will test that PlaylistFilter.all does not exclude any playlists
        // Implementation in Task 1 will make PlaylistFilter enum available
        XCTFail("Wave 0 stub — implement PlaylistFilter enum first")
    }

    func testFilterAnalyzedReturnsOnlyAnalyzed() {
        // Stub: Will test that .analyzed filter returns only playlists with coverageData entries
        XCTFail("Wave 0 stub — implement PlaylistFilter enum first")
    }

    func testFilterUnanalyzedReturnsOnlyUnanalyzed() {
        // Stub: Will test that .unanalyzed filter returns only playlists without coverageData entries
        XCTFail("Wave 0 stub — implement PlaylistFilter enum first")
    }

    // MARK: - Search Filtering (LIB-01)

    func testSearchFiltersPlaylistsByName() {
        // Stub: Will test that searchText filters playlists by case-insensitive name match
        XCTFail("Wave 0 stub — implement filteredPlaylists first")
    }

    func testSearchAndFilterCompound() {
        // Stub: Will test that search + filter stack (e.g., "Analyzed" + search "run")
        XCTFail("Wave 0 stub — implement filteredPlaylists first")
    }
}
```

**2. Create `BeatStepTests/PlaylistCoverageTests.swift`** (covers LIB-03):
```swift
import XCTest
@testable import BeatStep

final class PlaylistCoverageTests: XCTestCase {

    // MARK: - Percentage Calculation

    func testPercentageCalculation() {
        // Stub: PlaylistCoverage(tracksWithBPM: 42, totalTracks: 50).percentage == 0.84
        XCTFail("Wave 0 stub — implement PlaylistCoverage struct first")
    }

    func testPercentageZeroTotalTracks() {
        // Stub: PlaylistCoverage(tracksWithBPM: 0, totalTracks: 0).percentage == 0
        XCTFail("Wave 0 stub — implement PlaylistCoverage struct first")
    }

    // MARK: - Status Color Thresholds

    func testStatusColorGreenAbove80() {
        // Stub: 85% -> stateSuccess
        XCTFail("Wave 0 stub — implement PlaylistCoverage struct first")
    }

    func testStatusColorYellow40To80() {
        // Stub: 50% -> stateWarning
        XCTFail("Wave 0 stub — implement PlaylistCoverage struct first")
    }

    func testStatusColorRedBelow40() {
        // Stub: 20% -> stateError
        XCTFail("Wave 0 stub — implement PlaylistCoverage struct first")
    }

    // MARK: - Text Format

    func testTextFormat() {
        // Stub: PlaylistCoverage(tracksWithBPM: 42, totalTracks: 50).text == "42/50 BPM"
        XCTFail("Wave 0 stub — implement PlaylistCoverage struct first")
    }
}
```

**3. Add `testDeleteScan` stub to `BeatStepTests/LibraryScanServiceTests.swift`** (covers LIB-04):
Add this test method in a new `// MARK: - Delete Scan` section, before the `// MARK: - Helpers` section:
```swift
// MARK: - Delete Scan

func testDeleteScan() {
    // Stub: Will test that deleteScan removes ScannedPlaylist from SwiftData context
    // 1. Insert a ScannedPlaylist into the in-memory container
    // 2. Call scanService.deleteScan(playlistID:)
    // 3. Verify the ScannedPlaylist no longer exists
    XCTFail("Wave 0 stub — implement deleteScan method first")
}
```

**Important:** All stubs use `XCTFail()` so they compile but fail. Tasks 1 and 2 will replace these stubs with real test logic. Add the new test files to the BeatStepTests target in the Xcode project (use the same file reference pattern as existing test files -- check the `.pbxproj` for the existing pattern; if auto-discovery is used, just creating the files in the BeatStepTests directory is sufficient).
  </action>
  <verify>
    <automated>xcodebuild build-for-testing -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' -quiet 2>&1 | tail -5</automated>
  </verify>
  <done>Three test files compile: PlaylistFilterTests.swift (5 stubs), PlaylistCoverageTests.swift (6 stubs), and testDeleteScan added to LibraryScanServiceTests.swift (1 stub). All stubs XCTFail when run. build-for-testing succeeds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Data model, filtering logic, and design token</name>
  <files>BeatStep/Views/Library/PlaylistListView.swift, BeatStep/DesignSystem/DesignTokens.swift, BeatStepTests/PlaylistFilterTests.swift, BeatStepTests/PlaylistCoverageTests.swift</files>
  <behavior>
    - PlaylistCoverage(tracksWithBPM: 42, totalTracks: 50).percentage == 0.84
    - PlaylistCoverage(tracksWithBPM: 0, totalTracks: 0).percentage == 0
    - 85% coverage -> stateSuccess color
    - 50% coverage -> stateWarning color
    - 20% coverage -> stateError color
    - PlaylistCoverage text == "42/50 BPM"
    - PlaylistFilter.all returns all playlists
    - PlaylistFilter.analyzed returns only playlists with coverageData
    - PlaylistFilter.unanalyzed returns only playlists without coverageData
    - Search filters by case-insensitive name
    - Search + filter compound correctly
  </behavior>
  <action>
**In DesignTokens.swift** -- add to ComponentSize enum:
```swift
static let coverArtMedium: CGFloat = 56
```

**In PlaylistListView.swift** -- add these types and refactor the data layer (keep existing UI rendering unchanged for now -- Plan 02 handles the visual redesign):

1. Add `PlaylistCoverage` struct (above PlaylistListView):
```swift
struct PlaylistCoverage {
    let tracksWithBPM: Int
    let totalTracks: Int

    var percentage: Double {
        guard totalTracks > 0 else { return 0 }
        return Double(tracksWithBPM) / Double(totalTracks)
    }

    var statusColor: Color {
        switch percentage {
        case 0.8...: return .stateSuccess
        case 0.4...: return .stateWarning
        default:     return .stateError
        }
    }

    var text: String { "\(tracksWithBPM)/\(totalTracks) BPM" }
}
```

2. Add `PlaylistFilter` enum:
```swift
enum PlaylistFilter: String, CaseIterable {
    case all = "All"
    case analyzed = "Analyzed"
    case unanalyzed = "Unanalyzed"
}
```

3. Replace state property:
- Change `@State private var coverageMap: [String: String] = [:]` to `@State private var coverageData: [String: PlaylistCoverage] = [:]`
- Add `@State private var searchText = ""`
- Add `@State private var activeFilter: PlaylistFilter = .all`

4. Add `filteredPlaylists` computed property:
```swift
private var filteredPlaylists: [SpotifyPlaylist] {
    var result = playlists

    switch activeFilter {
    case .all: break
    case .analyzed:
        result = result.filter { coverageData[$0.id] != nil }
    case .unanalyzed:
        result = result.filter { coverageData[$0.id] == nil }
    }

    if !searchText.isEmpty {
        result = result.filter { $0.name.localizedCaseInsensitiveContains(searchText) }
    }

    return result
}
```

5. Update `loadCoverageData()` to build `[String: PlaylistCoverage]` instead of `[String: String]`:
```swift
private func loadCoverageData() {
    let context = BPMCacheService.shared.context
    let descriptor = FetchDescriptor<ScannedPlaylist>()
    guard let scannedPlaylists = try? context.fetch(descriptor) else { return }

    let scannedMap = Dictionary(
        scannedPlaylists.map { ($0.spotifyPlaylistID, $0) },
        uniquingKeysWith: { first, _ in first }
    )

    var newCoverageData: [String: PlaylistCoverage] = [:]
    for playlist in playlists {
        if let sp = scannedMap[playlist.id] {
            newCoverageData[playlist.id] = PlaylistCoverage(
                tracksWithBPM: sp.tracksWithBPM,
                totalTracks: sp.totalTracks
            )
        }
    }
    coverageData = newCoverageData
    coverageLoaded = true
}
```

6. Update `PlaylistRow` to accept `PlaylistCoverage?` instead of `String?`:
- Change `var coverageText: String? = nil` to `var coverage: PlaylistCoverage? = nil`
- In the body, change `coverageText` references to use `coverage?.text`
- Keep the existing visual layout (50pt height, coverArtSmall) -- Plan 02 will redesign it

7. Update the `ForEach` callsite in `playlistList` to pass `coverage: coverageData[playlist.id]` instead of `coverageText: coverageMap[playlist.id]`

**Then replace Wave 0 stubs with real tests:**

**In PlaylistCoverageTests.swift** -- replace all XCTFail stubs with real assertions:
```swift
func testPercentageCalculation() {
    let coverage = PlaylistCoverage(tracksWithBPM: 42, totalTracks: 50)
    XCTAssertEqual(coverage.percentage, 0.84, accuracy: 0.001)
}

func testPercentageZeroTotalTracks() {
    let coverage = PlaylistCoverage(tracksWithBPM: 0, totalTracks: 0)
    XCTAssertEqual(coverage.percentage, 0)
}

func testStatusColorGreenAbove80() {
    let coverage = PlaylistCoverage(tracksWithBPM: 85, totalTracks: 100)
    XCTAssertEqual(coverage.statusColor, .stateSuccess)
}

func testStatusColorYellow40To80() {
    let coverage = PlaylistCoverage(tracksWithBPM: 50, totalTracks: 100)
    XCTAssertEqual(coverage.statusColor, .stateWarning)
}

func testStatusColorRedBelow40() {
    let coverage = PlaylistCoverage(tracksWithBPM: 20, totalTracks: 100)
    XCTAssertEqual(coverage.statusColor, .stateError)
}

func testTextFormat() {
    let coverage = PlaylistCoverage(tracksWithBPM: 42, totalTracks: 50)
    XCTAssertEqual(coverage.text, "42/50 BPM")
}
```

**In PlaylistFilterTests.swift** -- replace XCTFail stubs with real tests. Since `filteredPlaylists` is a private computed property on PlaylistListView, test the underlying filter logic by testing PlaylistFilter enum behavior directly, and test the filtering function by extracting it or testing the types:
- For enum tests: verify `PlaylistFilter.allCases.count == 3`, verify raw values
- For filter logic tests: since the computed property is private to the view, write tests that verify the PlaylistFilter enum's `rawValue` and `CaseIterable` conformance, and test PlaylistCoverage integration with filter concepts (e.g., a playlist with coverage != nil is "analyzed"). Note: If filteredPlaylists cannot be tested in isolation due to being a private view property, document this and write the best approximation tests that verify the types and contracts the filter depends on.

**Important:** Do NOT change the ForEach to use `filteredPlaylists` yet or add `.searchable` -- that is Plan 02's work. This plan only adds the data types and computed property. The existing UI must still compile and function identically.
  </action>
  <verify>
    <automated>xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:BeatStepTests/PlaylistCoverageTests -only-testing:BeatStepTests/PlaylistFilterTests -quiet 2>&1 | tail -10</automated>
  </verify>
  <done>PlaylistCoverage struct with percentage/statusColor/text, PlaylistFilter enum with 3 cases, filteredPlaylists computed property, coverageData dictionary replacing coverageMap, coverArtMedium token = 56 -- all compiling, existing UI unchanged, PlaylistCoverageTests and PlaylistFilterTests all green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deleteScan method to LibraryScanService</name>
  <files>BeatStep/Services/LibraryScanService.swift, BeatStepTests/LibraryScanServiceTests.swift</files>
  <behavior>
    - deleteScan(playlistID:) removes ScannedPlaylist from SwiftData
    - deleteScan on nonexistent playlist is a no-op (no crash)
  </behavior>
  <action>
Add a `deleteScan` method to `LibraryScanService` that removes the `ScannedPlaylist` record for a given playlist ID:

```swift
// MARK: - Delete Scan

func deleteScan(playlistID: String) {
    let context = BPMCacheService.shared.context
    let descriptor = FetchDescriptor<ScannedPlaylist>(
        predicate: #Predicate { $0.spotifyPlaylistID == playlistID }
    )
    guard let existing = try? context.fetch(descriptor).first else { return }
    context.delete(existing)
    try? context.save()
}
```

Place this after the `scanEnabledPlaylists()` method and before the `// MARK: - ScannedPlaylist Updates` section.

This method:
- Fetches the ScannedPlaylist by spotifyPlaylistID
- Deletes it from the SwiftData context
- Saves the context
- No confirmation needed (per user decision -- re-scannable data, low risk)

**Then replace Wave 0 stub with real test in LibraryScanServiceTests.swift:**

Replace the `testDeleteScan` XCTFail stub with:
```swift
func testDeleteScan() {
    // Insert a ScannedPlaylist into the in-memory container
    let context = container.mainContext
    let scannedPlaylist = ScannedPlaylist(
        spotifyPlaylistID: "pl_delete_test",
        name: "Delete Test Playlist",
        totalTracks: 10,
        tracksWithBPM: 5,
        lastScanned: Date()
    )
    context.insert(scannedPlaylist)
    try? context.save()

    // Verify it exists
    let beforeDescriptor = FetchDescriptor<ScannedPlaylist>(
        predicate: #Predicate { $0.spotifyPlaylistID == "pl_delete_test" }
    )
    let beforeCount = (try? context.fetch(beforeDescriptor).count) ?? 0
    XCTAssertEqual(beforeCount, 1, "ScannedPlaylist should exist before delete")

    // Delete it
    scanService.deleteScan(playlistID: "pl_delete_test")

    // Verify it's gone
    let afterCount = (try? context.fetch(beforeDescriptor).count) ?? 0
    XCTAssertEqual(afterCount, 0, "ScannedPlaylist should be removed after deleteScan")
}
```

Note: Adapt the `ScannedPlaylist` initializer to match the actual model's init signature (check the SwiftData model definition). The test uses the existing in-memory ModelContainer from setUp().
  </action>
  <verify>
    <automated>xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:BeatStepTests/LibraryScanServiceTests/testDeleteScan -quiet 2>&1 | tail -10</automated>
  </verify>
  <done>deleteScan(playlistID:) method exists on LibraryScanService, compiles, follows existing SwiftData patterns, testDeleteScan passes green</done>
</task>

</tasks>

<verification>
- `xcodebuild test -scheme BeatStep -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:BeatStepTests/PlaylistCoverageTests -only-testing:BeatStepTests/PlaylistFilterTests -only-testing:BeatStepTests/LibraryScanServiceTests/testDeleteScan -quiet` -- all tests green
- PlaylistCoverage struct is accessible (not private) for future CoverageBar component
- PlaylistFilter enum is CaseIterable for ForEach iteration
- filteredPlaylists returns all playlists when filter=.all and searchText is empty
- deleteScan follows same SwiftData FetchDescriptor pattern as existing methods
</verification>

<success_criteria>
- All Wave 0 stubs created, then replaced with real tests that pass green
- Build succeeds with zero errors
- PlaylistCoverage provides percentage (Double), statusColor (Color), text (String)
- Color thresholds: green >80%, yellow 40-80%, red <40%
- PlaylistFilter has .all, .analyzed, .unanalyzed cases
- filteredPlaylists compounds search + filter
- coverageData replaces coverageMap throughout
- deleteScan removes ScannedPlaylist record
- coverArtMedium = 56 in ComponentSize
- Existing UI still renders identically (no visual changes yet)
</success_criteria>

<output>
After completion, create `.planning/phases/28-library-polish/28-01-SUMMARY.md`
</output>
